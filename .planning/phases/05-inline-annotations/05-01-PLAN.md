---
phase: 05-inline-annotations
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/src/tests/actionBubble.test.tsx
  - frontend/src/tests/citationBlock.test.tsx
  - frontend/src/tests/simplificationBlock.test.tsx
  - frontend/src/tests/annotationErrors.test.tsx
  - frontend/src/tests/messageBlock.test.tsx
  - frontend/src/tests/sessionStore.annotations.test.ts
autonomous: true
requirements:
  - INLINE-01
  - INLINE-02
  - INLINE-03
  - INLINE-04
  - INLINE-05
  - INLINE-06
  - INLINE-07
  - INLINE-08

must_haves:
  truths:
    - "Running `cd frontend && npx vitest run` exits 0 with all stub tests collected"
    - "Each stub file imports no module that does not yet exist (import-free test.todo pattern)"
    - "All 6 test files exist in frontend/src/tests/"
  artifacts:
    - path: "frontend/src/tests/actionBubble.test.tsx"
      provides: "Test stubs for INLINE-01 (Find Sources call), INLINE-05 (Simplify bubble expansion)"
    - path: "frontend/src/tests/citationBlock.test.tsx"
      provides: "Test stubs for INLINE-02 (citation note rendered), INLINE-03 (collapse/expand toggle)"
    - path: "frontend/src/tests/simplificationBlock.test.tsx"
      provides: "Test stubs for INLINE-06 (block below paragraph), INLINE-07 (try another mode)"
    - path: "frontend/src/tests/annotationErrors.test.tsx"
      provides: "Test stubs for INLINE-04 (inline error + retry)"
    - path: "frontend/src/tests/messageBlock.test.tsx"
      provides: "Test stubs for INLINE-08 (data-paragraph-id intact after annotation blocks)"
    - path: "frontend/src/tests/sessionStore.annotations.test.ts"
      provides: "Test stubs for updateAnnotation store action"
  key_links:
    - from: "frontend/src/tests/setup.ts"
      to: "frontend/vitest.config.ts"
      via: "setupFiles config — existing setup reused, no new setup needed"
      pattern: "setupFiles.*setup"
---

<objective>
Create Wave 0 test scaffold files so all subsequent plans have automated verify targets from day one. Uses the established `test.todo()` pattern (no imports of non-existent modules) so the suite stays green before implementation begins.

Purpose: Nyquist compliance — every automated verify command in plans 02–06 must reference a file that exists.
Output: 6 test files with `test.todo()` stubs; existing vitest suite stays green.
</objective>

<execution_context>
@C:/Users/bilge/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bilge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-inline-annotations/05-CONTEXT.md
@.planning/phases/05-inline-annotations/05-RESEARCH.md

<interfaces>
<!-- Established test.todo() pattern from Phase 4 (04-branching decision) -->
<!-- Wave 0 stubs use import-free test.todo() — safe before modules exist -->

From frontend/src/tests/setup.ts (existing, reuse as-is):
- Clerk mock and jsdom setup already configured
- No new setup file needed

Vitest config: frontend/vitest.config.ts
Quick run: cd frontend && npx vitest run --reporter=verbose 2>&1 | tail -20
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stub files for annotation components (actionBubble, citationBlock, simplificationBlock)</name>
  <files>frontend/src/tests/actionBubble.test.tsx, frontend/src/tests/citationBlock.test.tsx, frontend/src/tests/simplificationBlock.test.tsx</files>
  <action>
Create three test files using `import { test } from 'vitest';` only (no component imports — components don't exist yet). Each file uses `test.todo()` to register future test cases without executing them. This keeps the suite green while marking the Nyquist targets.

**frontend/src/tests/actionBubble.test.tsx** — stubs for INLINE-01, INLINE-05:
```tsx
import { test } from 'vitest';

// INLINE-01: Find Sources button calls searchSources() with anchorText
test.todo('ActionBubble: Find Sources button is enabled and calls onFindSources with anchorText and paragraphId');
test.todo('ActionBubble: Find Sources button calls e.preventDefault() on mousedown');

// INLINE-05: Simplify expands bubble in-place to 4 mode buttons + back arrow
test.todo('ActionBubble: Simplify button enters simplify mode showing 4 mode buttons');
test.todo('ActionBubble: back arrow in simplify mode returns to default 3-button view');
test.todo('ActionBubble: mode resets to default when paragraphId prop changes');
test.todo('ActionBubble: clicking outside bubble in simplify mode dismisses it entirely');
```

**frontend/src/tests/citationBlock.test.tsx** — stubs for INLINE-02, INLINE-03:
```tsx
import { test } from 'vitest';

// INLINE-03: collapsed by default, expands on toggle
test.todo('CitationBlock: renders collapsed pill "3 sources found" by default');
test.todo('CitationBlock: clicking toggle expands to show source rows');
test.todo('CitationBlock: expanded view shows title, domain badge, and link for each source');

// INLINE-02: citation note from Gemini shown when expanded
test.todo('CitationBlock: citation note text rendered below sources when expanded');
test.todo('CitationBlock: source title links open in new tab (target=_blank)');
```

**frontend/src/tests/simplificationBlock.test.tsx** — stubs for INLINE-06, INLINE-07:
```tsx
import { test } from 'vitest';

// INLINE-06: simplification block below paragraph, both visible
test.todo('SimplificationBlock: renders header with mode label and "Try another mode" button');
test.todo('SimplificationBlock: renders replacement text in tinted block');
test.todo('SimplificationBlock: has left border accent styling');

// INLINE-07: try another mode updates existing annotation, no duplicate block
test.todo('SimplificationBlock: "Try another mode" calls onTryAnother callback');
test.todo('updateAnnotation: updates replacementText on existing annotation without adding duplicate');
test.todo('updateAnnotation: does not mutate other annotations in the same message');
```
  </action>
  <verify>
    <automated>cd "C:/gmu/coding/GenAI Web interface/child_chats_v1/frontend" && npx vitest run src/tests/actionBubble.test.tsx src/tests/citationBlock.test.tsx src/tests/simplificationBlock.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All 3 files exist; vitest reports them as collected (todo tests shown); no import errors; suite exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Create test stub files for error handling, MessageBlock, and store annotations</name>
  <files>frontend/src/tests/annotationErrors.test.tsx, frontend/src/tests/messageBlock.test.tsx, frontend/src/tests/sessionStore.annotations.test.ts</files>
  <action>
Create three more test stub files using the same `test.todo()` pattern.

**frontend/src/tests/annotationErrors.test.tsx** — stubs for INLINE-04:
```tsx
import { test } from 'vitest';

// INLINE-04: inline error block with retry on API failure
test.todo('AnnotationError: renders inline error block when Find Sources API fails');
test.todo('AnnotationError: error block shows "Couldn\'t load sources" message');
test.todo('AnnotationError: Retry button re-triggers the API call');
test.todo('AnnotationError: renders inline error block when Simplify API fails');
test.todo('AnnotationError: error block disappears and shimmer appears on retry click');
```

**frontend/src/tests/messageBlock.test.tsx** — stubs for INLINE-08:
```tsx
import { test } from 'vitest';

// INLINE-08: annotation blocks rendered outside data-paragraph-id elements
test.todo('MessageBlock: data-paragraph-id attributes on paragraphs are intact after annotation blocks render');
test.todo('MessageBlock: CitationBlock is rendered outside the AI bubble div');
test.todo('MessageBlock: SimplificationBlock is rendered outside the AI bubble div');
test.todo('MessageBlock: annotation blocks are siblings of bubble div, not children');
```

**frontend/src/tests/sessionStore.annotations.test.ts** — stubs for updateAnnotation action:
```ts
import { test } from 'vitest';

// updateAnnotation action (new in Phase 5)
test.todo('updateAnnotation: applies Partial<Annotation> patch to matching annotation by id');
test.todo('updateAnnotation: leaves other annotations in the message unchanged');
test.todo('updateAnnotation: no-op when annotationId does not exist in message');
test.todo('updateAnnotation: produces new object reference (immutable update)');
```
  </action>
  <verify>
    <automated>cd "C:/gmu/coding/GenAI Web interface/child_chats_v1/frontend" && npx vitest run src/tests/annotationErrors.test.tsx src/tests/messageBlock.test.tsx src/tests/sessionStore.annotations.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All 3 files exist; vitest reports them as todo stubs; no errors; full suite run exits 0.</done>
</task>

</tasks>

<verification>
Full suite check after Wave 0:
`cd "C:/gmu/coding/GenAI Web interface/child_chats_v1/frontend" && npx vitest run 2>&1 | tail -10`

Expected: all existing tests pass, new todo tests collected without error. Exit code 0.
</verification>

<success_criteria>
- 6 test files exist in frontend/src/tests/
- `npx vitest run` exits 0 (no import errors, no failures)
- Todo stubs are registered under their correct requirement groupings
- No imports of modules that do not yet exist
</success_criteria>

<output>
After completion, create `.planning/phases/05-inline-annotations/05-01-SUMMARY.md`
</output>
