---
phase: 05-inline-annotations
plan: 04
type: execute
wave: 2
depends_on:
  - 05-02
  - 05-03
files_modified:
  - frontend/src/components/annotations/CitationBlock.tsx
  - frontend/src/components/annotations/AnnotationShimmer.tsx
autonomous: true
requirements:
  - INLINE-02
  - INLINE-03
  - INLINE-04

must_haves:
  truths:
    - "CitationBlock renders a collapsed pill 'X sources found' by default"
    - "Clicking the toggle expands to show source rows (title link, domain badge) and Gemini citation note"
    - "AnnotationShimmer renders a shimmer placeholder matching CitationBlock border/shape via Tailwind animate-pulse"
    - "Source title links open in a new tab (target=_blank rel=noreferrer)"
  artifacts:
    - path: "frontend/src/components/annotations/CitationBlock.tsx"
      provides: "Collapsible citation block with source rows and Gemini note"
      exports: ["CitationBlock"]
    - path: "frontend/src/components/annotations/AnnotationShimmer.tsx"
      provides: "Shared shimmer placeholder for source and simplification loading states"
      exports: ["AnnotationShimmer"]
  key_links:
    - from: "frontend/src/components/annotations/CitationBlock.tsx"
      to: "frontend/src/types/index.ts"
      via: "Annotation type (type === 'source') — sources[] and citationNote fields"
      pattern: "Annotation"
---

<objective>
Build the CitationBlock presentational component and the shared AnnotationShimmer. CitationBlock is collapsible (default collapsed), shows source rows with title/domain/link, and displays the Gemini-generated note when expanded.

Purpose: CitationBlock is the visual output of Find Sources. AnnotationShimmer is shared by both annotation types during loading. Both are purely presentational — no async logic.
Output: Two new components in frontend/src/components/annotations/.
</objective>

<execution_context>
@C:/Users/bilge/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bilge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-inline-annotations/05-CONTEXT.md
@.planning/phases/05-inline-annotations/05-RESEARCH.md

<interfaces>
<!-- Types CitationBlock consumes -->
```typescript
// frontend/src/types/index.ts
export interface Annotation {
  id: string;
  type: 'source' | 'rewrite' | 'simplification';
  targetText: string;
  paragraphIndex: number;
  originalText: string;
  replacementText: string | null;
  citationNote: string | null;         // Gemini-generated note (may be '' if generation failed)
  sources: SourceResult[];
  isShowingOriginal: boolean;
}

export interface SourceResult {
  title: string;
  url: string;
  domain: string;
  snippet: string;
}
```

<!-- Dark theme colors (zinc palette — established project convention) -->
// Background: zinc-900 (main), zinc-800 (cards), zinc-700 (interactive elements)
// Border: zinc-700 / zinc-600
// Text: slate-200 (primary), slate-400 (muted)

<!-- AnnotationShimmer pattern from research -->
// Uses Tailwind animate-pulse on placeholder divs
// Same border/shape as the final block to prevent layout shift
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build CitationBlock component</name>
  <files>frontend/src/components/annotations/CitationBlock.tsx</files>
  <behavior>
    - Test (citationBlock.test.tsx): renders collapsed pill "N sources found" by default when sources.length > 0
    - Test (citationBlock.test.tsx): clicking toggle expands to show source rows
    - Test (citationBlock.test.tsx): source title is a link with target=_blank
    - Test (citationBlock.test.tsx): domain badge shown next to each source
    - Test (citationBlock.test.tsx): citationNote text rendered below sources when expanded (when non-empty)
    - Test (citationBlock.test.tsx): toggle button text is "▼" collapsed, "▲" expanded
  </behavior>
  <action>
Create `frontend/src/components/annotations/CitationBlock.tsx`. Create the directory `frontend/src/components/annotations/` first if it does not exist.

```tsx
/**
 * CitationBlock
 *
 * Collapsible citation block rendered below an AI message bubble.
 * Default state: collapsed (single-line pill).
 * Expanded: source rows + Gemini-generated note.
 *
 * Requirements: INLINE-02, INLINE-03
 *
 * CRITICAL: Must be rendered OUTSIDE the MarkdownRenderer div in MessageBlock
 * so it does not interfere with data-paragraph-id elements.
 */
import { useState } from 'react';
import type { Annotation } from '../../types/index';

interface CitationBlockProps {
  annotation: Annotation; // type must be 'source'
}

export function CitationBlock({ annotation }: CitationBlockProps) {
  const [expanded, setExpanded] = useState(false);
  const { sources, citationNote } = annotation;
  const count = sources.length;

  return (
    <div className="mt-2 max-w-[720px] mx-auto rounded-lg border border-zinc-700 bg-zinc-800 text-sm">
      {/* Header row — always visible */}
      <button
        className="w-full flex items-center justify-between px-3 py-2 text-slate-300 hover:text-slate-100 transition-colors"
        onClick={() => setExpanded(prev => !prev)}
        aria-expanded={expanded}
        aria-label={expanded ? 'Collapse sources' : 'Expand sources'}
      >
        <span>🔎 {count} source{count !== 1 ? 's' : ''} found</span>
        <span className="text-xs text-slate-400">{expanded ? '▲' : '▼'}</span>
      </button>

      {/* Expanded content */}
      {expanded && (
        <div className="border-t border-zinc-700 px-3 py-2 space-y-2">
          {/* Source rows */}
          {sources.map((source, i) => (
            <div key={i} className="flex items-start gap-2">
              <a
                href={source.url}
                target="_blank"
                rel="noreferrer"
                className="text-blue-400 hover:text-blue-300 underline flex-1 min-w-0 truncate"
                title={source.title}
              >
                ↗ {source.title}
              </a>
              <span className="shrink-0 text-xs text-slate-400 bg-zinc-700 px-1.5 py-0.5 rounded">
                {source.domain}
              </span>
            </div>
          ))}

          {/* Horizontal divider before Gemini note */}
          {citationNote && (
            <>
              <hr className="border-zinc-700" />
              <p className="text-slate-300 text-xs leading-relaxed">
                💬 {citationNote}
              </p>
            </>
          )}
        </div>
      )}
    </div>
  );
}
```

Style notes (Claude's discretion per CONTEXT.md):
- Dark theme: zinc-800 background, zinc-700 border — matches existing dark theme palette
- Source links: blue-400 (consistent with Go Deeper button blue accent)
- No dismiss/remove button on CitationBlock — annotation persists for the session
  </action>
  <verify>
    <automated>cd "C:/gmu/coding/GenAI Web interface/child_chats_v1/frontend" && npx vitest run src/tests/citationBlock.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - CitationBlock file exists and exports `CitationBlock`
    - Default state is collapsed (shows pill text)
    - Clicking toggle expands source list
    - Source titles are anchor tags with target=_blank and rel=noreferrer
    - Domain badge visible per source
    - Gemini note shown below divider when expanded and citationNote non-empty
    - TypeScript clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Build AnnotationShimmer shared loading placeholder</name>
  <files>frontend/src/components/annotations/AnnotationShimmer.tsx</files>
  <action>
Create `frontend/src/components/annotations/AnnotationShimmer.tsx`.

The shimmer must match the border/shape of the final blocks to prevent layout shift when real content arrives. Two variants: `source` (3 rows shimmer) and `simplification` (1 block shimmer).

```tsx
/**
 * AnnotationShimmer
 *
 * Shared loading placeholder for citation and simplification blocks.
 * Uses Tailwind animate-pulse. Matches the border and shape of the final blocks
 * to prevent layout shift on load completion.
 *
 * Requirements: INLINE-01 (loading state), INLINE-05 (loading state)
 */
interface AnnotationShimmerProps {
  type: 'source' | 'simplification';
}

export function AnnotationShimmer({ type }: AnnotationShimmerProps) {
  return (
    <div className="mt-2 max-w-[720px] mx-auto rounded-lg border border-zinc-700 bg-zinc-800 p-3 animate-pulse">
      {/* Header placeholder */}
      <div className="h-3 bg-zinc-600 rounded w-1/3 mb-3" />

      {type === 'source' && (
        <>
          <div className="h-3 bg-zinc-600 rounded w-full mb-2" />
          <div className="h-3 bg-zinc-600 rounded w-4/5 mb-2" />
          <div className="h-3 bg-zinc-600 rounded w-full mb-2" />
          <hr className="border-zinc-700 my-2" />
          <div className="h-3 bg-zinc-600 rounded w-2/3" />
        </>
      )}

      {type === 'simplification' && (
        <>
          <div className="h-3 bg-zinc-600 rounded w-3/4 mb-2" />
          <div className="h-10 bg-zinc-600 rounded w-full" />
        </>
      )}
    </div>
  );
}
```

Note: Uses `zinc-600` for shimmer bars (lighter than `zinc-800` background, darker than content text) to maintain visual contrast in dark theme. This is Claude's discretion per CONTEXT.md.
  </action>
  <verify>
    <automated>cd "C:/gmu/coding/GenAI Web interface/child_chats_v1/frontend" && npx tsc --noEmit 2>&1 | grep "annotations/AnnotationShimmer" | head -5</automated>
  </verify>
  <done>
    - AnnotationShimmer.tsx exists and exports `AnnotationShimmer`
    - Accepts `type: 'source' | 'simplification'` prop
    - Uses `animate-pulse` for shimmer
    - Border/shape matches CitationBlock (zinc-700 border, zinc-800 bg)
    - TypeScript clean (no errors)
  </done>
</task>

</tasks>

<verification>
Full frontend test suite:
`cd "C:/gmu/coding/GenAI Web interface/child_chats_v1/frontend" && npx vitest run 2>&1 | tail -10`

TypeScript check:
`cd "C:/gmu/coding/GenAI Web interface/child_chats_v1/frontend" && npx tsc --noEmit 2>&1 | head -20`
</verification>

<success_criteria>
- `frontend/src/components/annotations/` directory exists with CitationBlock.tsx and AnnotationShimmer.tsx
- CitationBlock: collapsed by default, expands on toggle, source links open new tab, domain badge visible, citation note shown
- AnnotationShimmer: animate-pulse, matches block shape, two variants (source/simplification)
- TypeScript clean; vitest suite exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/05-inline-annotations/05-04-SUMMARY.md`
</output>
