---
phase: 05-inline-annotations
plan: 07
type: execute
wave: 4
depends_on:
  - 05-06
files_modified: []
autonomous: false
requirements:
  - INLINE-01
  - INLINE-02
  - INLINE-03
  - INLINE-04
  - INLINE-05
  - INLINE-06
  - INLINE-07
  - INLINE-08

must_haves:
  truths:
    - "User can click Find Sources on a paragraph and see a citation block with 3 sources and Gemini note"
    - "Citation block is collapsed by default; clicking toggle expands it"
    - "User can click Simplify, pick a mode, and see the simplification block below the paragraph"
    - "Both original paragraph and simplification block are visible simultaneously"
    - "Error block with Retry appears when API call fails"
    - "Annotated text is re-selectable; action bubble offers all 3 actions"
    - "Gutter pills do not shift position after annotation blocks are injected"
  artifacts:
    - path: "frontend/src/components/annotations/CitationBlock.tsx"
      provides: "Collapsible citation block — verified working by human"
    - path: "frontend/src/components/annotations/SimplificationBlock.tsx"
      provides: "Simplification block below paragraph — verified working by human"
  key_links:
    - from: "ActionBubble (Find Sources / Simplify buttons)"
      to: "CitationBlock / SimplificationBlock (rendered in MessageBlock)"
      via: "Full async flow through ThreadView handlers → store → MessageBlock render"
      pattern: "end-to-end"
---

<objective>
Human verification of all 8 INLINE requirements. Automated tests cover unit behavior; this checkpoint verifies the integrated end-to-end experience that requires real browser interaction and visual assessment.

Purpose: Inline annotation features involve DOM text selection, real API calls, dynamic rendering, and visual layout — behaviors that cannot be fully validated by jsdom unit tests.
Output: Human approval that all INLINE requirements work correctly in the browser.
</objective>

<execution_context>
@C:/Users/bilge/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bilge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-inline-annotations/05-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete inline annotations feature:
- Find Sources: ActionBubble → Tavily search → CitationBlock with title/domain/link + Gemini note (collapsed by default)
- Simplify: ActionBubble 4-mode picker → Gemini rewrite → SimplificationBlock below paragraph (both visible)
- Loading: shimmer placeholder during API calls
- Error: inline error block with Retry on failure
- Re-selectability: annotated paragraphs remain selectable for all 3 actions
  </what-built>
  <how-to-verify>
**Setup:**
1. Start backend: `cd backend && npm run dev`
2. Start frontend: `cd frontend && npm run dev`
3. Open the app in the browser and start or continue a conversation in the root thread

**INLINE-01, INLINE-02, INLINE-03 — Find Sources:**
1. Wait for an AI response to finish streaming
2. Select text from one AI paragraph (click and drag)
3. The ActionBubble should appear with Go Deeper, Find Sources, Simplify — all 3 enabled
4. Click "Find Sources"
5. Expected: shimmer placeholder appears below the message bubble
6. Expected: shimmer is replaced by a collapsed pill "🔎 3 sources found ▼"
7. Click the pill to expand it
8. Expected: 3 source rows appear — each with a clickable title (opens new tab), domain badge
9. Expected: Gemini citation note appears below a divider with 💬 prefix
10. Verify: click a source title → opens in new tab

**INLINE-04 — Error handling:**
1. Disable network or stop the backend
2. Select text and click "Find Sources"
3. Expected: shimmer appears, then inline error block: "⚠ Couldn't load sources — [Retry]"
4. Re-enable network, click Retry
5. Expected: shimmer reappears, then citation block loads

**INLINE-05, INLINE-06 — Simplify:**
1. Ensure backend is running
2. Select text from an AI paragraph
3. Click "Simplify" in the action bubble
4. Expected: bubble morphs in-place to show 4 mode buttons (Simpler, Example, Analogy, Technical) plus "← Back"
5. Click "← Back"
6. Expected: bubble returns to 3-button default view
7. Select text again, click Simplify, click "Simpler"
8. Expected: shimmer appears below the paragraph, then a SimplificationBlock appears
9. Expected: original paragraph is still fully visible above the block (NOT replaced)
10. Expected: block header shows "✎ Simplified • Simpler"

**INLINE-07 — Try another mode:**
1. With a SimplificationBlock visible, click "Try another mode" inside the block
2. Expected: 4 mode buttons appear inline within the block
3. Click "Analogy"
4. Expected: shimmer briefly replaces the block, then block updates to "✎ Simplified • Analogy"
5. Expected: only ONE SimplificationBlock per paragraph (no duplicate)

**INLINE-08 — Re-selectability and gutter pill stability:**
1. Select text from a paragraph that has a CitationBlock below it
2. Expected: ActionBubble appears with all 3 actions (Go Deeper, Find Sources, Simplify)
3. Click "Go Deeper" — new child thread should open normally
4. Return to parent thread
5. Expected: gutter pill (if any) is still aligned to its anchor paragraph, not shifted
6. Select text from a paragraph that has a SimplificationBlock below it
7. Expected: same — ActionBubble with all 3 actions, able to branch
  </how-to-verify>
  <resume-signal>Type "approved" when all verifications pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Automated suite final gate (run before considering this checkpoint):
`cd "C:/gmu/coding/GenAI Web interface/child_chats_v1/frontend" && npx vitest run 2>&1 | tail -5`

All tests must be green before the human checkpoint begins.
</verification>

<success_criteria>
- Human confirms "approved"
- All 8 INLINE requirements verified working in the browser
- No visual regressions: gutter pills still aligned, navigation still smooth
- Full vitest suite green
</success_criteria>

<output>
After completion, create `.planning/phases/05-inline-annotations/05-07-SUMMARY.md`
</output>
