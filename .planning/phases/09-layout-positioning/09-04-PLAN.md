---
phase: 09-layout-positioning
plan: 04
type: execute
wave: 1
depends_on: ["09-03"]
files_modified: [frontend/src/components/branching/GutterColumn.tsx]
autonomous: true
gap_closure: true
requirements: [PILL-01, PILL-02, PILL-03, PILL-04, PILL-05, PILL-06, PILL-07, PILL-08, ANCS-01, ANCS-02, ANCS-03, ANCS-04, ANCS-05, ANCS-06]
must_haves:
  truths:
    - "No text-[10px] appears in any phase-9-modified component file"
    - "DescendantPill arrow glyph and count spans use text-xs (12px)"
  artifacts:
    - path: "frontend/src/components/branching/GutterColumn.tsx"
      provides: "DescendantPill with ANCS-06-compliant text sizing"
      contains: "text-xs"
  key_links: []
---

<objective>
Fix ANCS-06 gap: replace two remaining `text-[10px]` instances in GutterColumn.tsx DescendantPill with `text-xs` to satisfy the phase-wide minimum readable text size requirement.

Purpose: VERIFICATION.md found GutterColumn.tsx lines 98 and 100 still use text-[10px] in the DescendantPill sub-component (arrow glyph and message count spans). ANCS-06 requires no text-[10px] anywhere in phase-modified components.
Output: GutterColumn.tsx with all text sizes at text-xs minimum.
</objective>

<execution_context>
@C:/Users/bilge/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bilge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-layout-positioning/09-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace text-[10px] with text-xs in DescendantPill</name>
  <files>frontend/src/components/branching/GutterColumn.tsx</files>
  <action>
In GutterColumn.tsx, inside the DescendantPill component, make two changes:

1. Line 98: Change `text-[10px]` to `text-xs` on the arrow glyph span (the one containing the ↳ character entity).
   Before: `className="text-slate-300 dark:text-zinc-500 flex-shrink-0 text-[10px]"`
   After:  `className="text-slate-300 dark:text-zinc-500 flex-shrink-0 text-xs"`

2. Line 100: Change `text-[10px]` to `text-xs` on the message count span.
   Before: `className="text-[10px] text-slate-400 dark:text-zinc-500 flex-shrink-0"`
   After:  `className="text-xs text-slate-400 dark:text-zinc-500 flex-shrink-0"`

No other changes needed. This closes the ANCS-06 gap identified in VERIFICATION.md.
  </action>
  <verify>
    <automated>cd frontend && grep -n "text-\[10px\]" src/components/branching/GutterColumn.tsx; echo "EXIT:$?"</automated>
  </verify>
  <done>grep returns no matches (exit code 1 from grep). Zero instances of text-[10px] remain in GutterColumn.tsx. All existing tests still pass: cd frontend && npx vitest run --reporter=verbose</done>
</task>

</tasks>

<verification>
1. `grep -rn "text-\[10px\]" frontend/src/components/` returns no matches across all phase-modified component files
2. `cd frontend && npx vitest run --reporter=verbose` — all tests pass (no regressions)
</verification>

<success_criteria>
- Zero instances of text-[10px] in GutterColumn.tsx
- Zero instances of text-[10px] in any phase-9-modified component file
- All 50 phase-9 tests continue to pass
- ANCS-06 requirement fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/09-layout-positioning/09-04-SUMMARY.md`
</output>
