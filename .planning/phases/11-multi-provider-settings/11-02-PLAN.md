---
phase: 11-multi-provider-settings
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - backend/src/providers/anthropic.ts
  - backend/src/providers/openai-search.ts
  - backend/src/config.ts
  - backend/src/routes/verify-key.ts
  - backend/src/routes/index.ts
  - backend/src/middleware/byokRateLimiter.ts
  - backend/tests/providers.test.ts
  - backend/tests/verifyKey.test.ts
  - backend/tests/byokRateLimiter.test.ts
  - backend/tests/sanitizeBody.test.ts
  - backend/package.json
autonomous: true
requirements: [BKND-06, BKND-07, BKND-08, BKND-09, BKND-10, BKND-11, BKND-12]

must_haves:
  truths:
    - "AnthropicProvider implements AIProvider with streamChat, simplify, and generateCitationNote"
    - "POST /api/verify-key validates key format and makes lightweight test call per provider"
    - "API keys are never logged — scrubbed from request body before any middleware sees them"
    - "Error responses redact any API key substrings"
    - "BYOK requests rate-limited to 30 req/min per user"
    - "CORS is restricted to CLIENT_ORIGIN app domain only (not wildcard)"
  artifacts:
    - path: "backend/src/providers/anthropic.ts"
      provides: "AnthropicProvider implementing AIProvider"
      contains: "class AnthropicProvider"
    - path: "backend/src/routes/verify-key.ts"
      provides: "POST /api/verify-key endpoint"
      exports: ["verifyKeyRouter"]
    - path: "backend/src/middleware/byokRateLimiter.ts"
      provides: "Per-user BYOK rate limiter"
      exports: ["byokRateLimiter"]
  key_links:
    - from: "backend/src/config.ts"
      to: "backend/src/providers/anthropic.ts"
      via: "new AnthropicProvider(apiKey, model) in createByokProvider"
      pattern: "new AnthropicProvider"
    - from: "backend/src/routes/index.ts"
      to: "backend/src/routes/verify-key.ts"
      via: "apiRouter.use('/verify-key', verifyKeyRouter)"
      pattern: "verify-key"
---

<objective>
Add the Anthropic Claude provider, create the key verification endpoint, implement BYOK security middleware (key sanitization, format validation, per-user rate limiting), and verify CORS restriction.

Purpose: Complete the backend provider set and ensure BYOK requests are secure.
Output: Working Anthropic provider, /api/verify-key endpoint, security middleware, passing tests.
</objective>

<execution_context>
@C:/Users/bilge/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bilge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-multi-provider-settings/11-CONTEXT.md
@.planning/phases/11-multi-provider-settings/11-RESEARCH.md
@.planning/phases/11-multi-provider-settings/11-01-SUMMARY.md

<interfaces>
<!-- Contracts from plan 11-01 that this plan builds on -->

From backend/src/config.ts (after 11-01):
```typescript
export function getDefaultProvider(): AIProvider;
export function getDefaultSearchProvider(): SearchProvider;
export function createByokProvider(
  provider: 'gemini' | 'openai' | 'anthropic',
  model: string,
  apiKey: string
): AIProvider;
```

From backend/src/providers/types.ts:
```typescript
export interface AIProvider {
  streamChat(messages: Message[], systemPrompt: string, onChunk, onDone, onError, signal?): Promise<void>;
  simplify(text: string, mode: string): Promise<string>;
  generateCitationNote(results: SearchResult[], originalText: string): Promise<string>;
}
```

From backend/src/middleware/rateLimiter.ts (existing IP-based limiter pattern):
```typescript
export const apiRateLimiter = rateLimit({
  windowMs: 15 * 60 * 1000,
  limit: 100,
  keyGenerator: (req) => req.ip ?? 'anonymous',
  ...
});
```

From backend/src/routes/index.ts (route registration pattern):
```typescript
apiRouter.use(requireApiAuth);
apiRouter.use('/chat', chatRouter);
// ... more routes
```

From backend/src/index.ts (CORS config):
```typescript
app.use(cors({
  origin: process.env.CLIENT_ORIGIN ?? 'http://localhost:5173',
  ...
}));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement AnthropicProvider and wire into config.ts factory</name>
  <files>backend/src/providers/anthropic.ts, backend/src/config.ts, backend/tests/providers.test.ts, backend/package.json</files>
  <behavior>
    - Test: AnthropicProvider(apiKey, model) creates client
    - Test: streamChat maps 'model' role to 'assistant' in messages array
    - Test: streamChat passes systemPrompt as top-level 'system' param (NOT in messages array)
    - Test: simplify returns content from response.content[0].text
    - Test: generateCitationNote returns synthesized note
    - Test: createByokProvider('anthropic', 'claude-sonnet-4-6', 'sk-ant-...') returns AnthropicProvider
  </behavior>
  <action>
    Install Anthropic SDK: `cd backend && npm install @anthropic-ai/sdk`

    1. Create backend/src/providers/anthropic.ts:
       - `constructor(apiKey: string, model: string)` creates `new Anthropic({ apiKey })`
       - `streamChat`: Use `client.messages.stream({ model, max_tokens: 4096, system: systemPrompt || undefined, messages: [...mapped] })`. Map role 'model' -> 'assistant'. Use `for await` on stream, check `event.type === 'content_block_delta' && event.delta.type === 'text_delta'` for chunks. Check `signal?.aborted` per iteration.
       - `simplify`: Non-streaming. `client.messages.create(...)`. Extract `response.content[0]` and check `block.type === 'text'` before returning `block.text`.
       - `generateCitationNote`: Same non-streaming pattern as simplify with citation prompt.
       - Copy SIMPLIFY_PROMPTS from gemini.ts (or extract to shared file if cleaner — Claude's discretion).

    PITFALL: Anthropic system prompt is TOP-LEVEL, not in messages. `messages.stream({ system: systemPrompt, messages: [...] })`.
    PITFALL: Role mapping: internal 'model' -> Anthropic 'assistant'.

    2. Update config.ts createByokProvider to handle 'anthropic':
       ```typescript
       if (provider === 'anthropic') return new AnthropicProvider(apiKey, model);
       ```

    3. Update backend/tests/providers.test.ts:
       - Add mock for @anthropic-ai/sdk
       - Test AnthropicProvider construction and role mapping
       - Test createByokProvider('anthropic', ...) returns AnthropicProvider
  </action>
  <verify>
    <automated>cd backend && npm test -- --testPathPattern=providers</automated>
  </verify>
  <done>AnthropicProvider fully implements AIProvider, createByokProvider handles all 3 providers, tests pass with mocked SDK</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create verify-key endpoint with format validation and security middleware</name>
  <files>backend/src/routes/verify-key.ts, backend/src/routes/index.ts, backend/src/middleware/byokRateLimiter.ts, backend/tests/verifyKey.test.ts, backend/tests/byokRateLimiter.test.ts, backend/tests/sanitizeBody.test.ts</files>
  <behavior>
    - Test: POST /api/verify-key with valid gemini key format returns { data: { valid: true } }
    - Test: POST /api/verify-key with malformed key (fails regex) returns { data: { valid: false, message: "Invalid key format..." } } without making API call
    - Test: POST /api/verify-key with missing provider/apiKey returns 400
    - Test: Key format regex validates: gemini (AIza...), openai (sk-...), anthropic (sk-ant-...)
    - Test: Error messages from SDK redact the API key string
    - Test: byokRateLimiter uses req.verifiedUser.sub as key, limits to 30/min
    - Test: API key is never present in logged/forwarded request body after extraction
  </behavior>
  <action>
    1. Create backend/src/routes/verify-key.ts:
       - POST handler accepts `{ provider, apiKey }`
       - First: key format validation via regex. Patterns:
         - gemini: `/^AIza[0-9A-Za-z\-_]{35}$/`
         - openai: `/^sk-[A-Za-z0-9]{20,}$/` (covers sk-proj- prefix too)
         - anthropic: `/^sk-ant-[A-Za-z0-9\-_]{32,}$/`
       - If format invalid, return `{ data: { valid: false, message: "Invalid key format for <provider>" }, error: null }` — no network call
       - If format valid, make lightweight test call per provider:
         - Gemini: `ai.models.generateContent({ model: 'gemini-2.0-flash', contents: 'hi', config: { maxOutputTokens: 1 } })`
         - OpenAI: `client.models.list()` (lists models, no cost)
         - Anthropic: `client.models.list()` (key validation, no cost)
       - On success: `{ data: { valid: true }, error: null }`
       - On failure: `{ data: { valid: false, message: sanitizedErrorMessage }, error: null }` — redact apiKey from error message via `msg.replace(apiKey, '[REDACTED]')`
       - Use dynamic import for SDKs (`await import(...)`) so they're not loaded unless needed

    2. Register in backend/src/routes/index.ts:
       - `import { verifyKeyRouter } from './verify-key.js';`
       - `apiRouter.use('/verify-key', verifyKeyRouter);` (after requireApiAuth — key verification requires auth)

    3. Create backend/src/middleware/byokRateLimiter.ts:
       - Use express-rate-limit with `windowMs: 60000, limit: 30`
       - `keyGenerator: (req) => req.verifiedUser?.sub ?? req.ip ?? 'anonymous'`
       - Return 429 with `{ data: null, error: { code: 'RATE_LIMITED', message: 'Too many requests...' } }`
       - Apply byokRateLimiter in verify-key route and conditionally in chat/simplify/find-sources when byok is present

    4. BKND-09 (error redaction): In verify-key.ts catch block, always run `msg.replace(apiKey, '[REDACTED]')` before sending error to client.

    5. BKND-12 (CORS): Verify that backend/src/index.ts CORS config uses `process.env.CLIENT_ORIGIN` (already confirmed — not wildcard). Add a brief comment confirming CORS is domain-restricted. If CLIENT_ORIGIN is not set in production .env, add a startup warning.

    6. Create test files:
       - backend/tests/verifyKey.test.ts: Test format validation regex for all 3 providers (valid/invalid patterns). Mock the actual SDK calls. Test error redaction.
       - backend/tests/byokRateLimiter.test.ts: Test keyGenerator extracts userId, test rate limit config values.
       - backend/tests/sanitizeBody.test.ts: Test that extracting rawApiKey and deleting from byok object works correctly (the pattern used in routes from 11-01).
  </action>
  <verify>
    <automated>cd backend && npm test</automated>
  </verify>
  <done>POST /api/verify-key works with format validation + lightweight test calls, BYOK rate limiter at 30/min per user, error messages redact keys, CORS confirmed domain-restricted, all security tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Implement OpenAISearchProvider for BYOK OpenAI web search</name>
  <files>backend/src/providers/openai-search.ts</files>
  <action>
    Replace the OpenAISearchProvider stub in backend/src/providers/openai-search.ts with a real implementation:

    1. `constructor(apiKey: string)` creates `new OpenAI({ apiKey })`
    2. `findSources(query, maxResults = 3)`: Use OpenAI's web search tool calling pattern:
       - Make a chat completion call with `tools: [{ type: 'web_search_preview' }]` or use the Responses API with web search
       - Parse results into SearchResult[] format matching the interface
       - If OpenAI web search tool calling proves too complex, implement as a simple completion that instructs GPT to search and return JSON — but prefer the native tool if available
    3. If the OpenAI web search tool is not straightforward (it uses the Responses API, not chat completions), implement a simpler fallback: `findSources` returns results from a chat completion that asks the model to provide factual URLs and snippets, clearly noting this is AI-generated search (not real web search). The UI will show this option only when user explicitly selects it.

    NOTE: This is a best-effort implementation. Tavily remains the default and recommended search provider. OpenAI Search is an option only when the user has an OpenAI BYOK key and explicitly selects it.
  </action>
  <verify>
    <automated>cd backend && npm test -- --testPathPattern=providers</automated>
  </verify>
  <done>OpenAISearchProvider has a real implementation (or documented limitation), replacing the stub</done>
</task>

</tasks>

<verification>
- `cd backend && npm test` — all tests pass
- `cd backend && npx tsc --noEmit` — no TypeScript errors
- All 3 providers (Gemini, OpenAI, Anthropic) can be instantiated via createByokProvider
- POST /api/verify-key accepts provider+apiKey and returns valid/invalid
</verification>

<success_criteria>
- AnthropicProvider fully implements AIProvider (streamChat, simplify, generateCitationNote)
- POST /api/verify-key endpoint validates key format and makes lightweight test call
- BYOK rate limiter at 30 req/min per userId
- Error messages never contain raw API keys
- CORS restricted to CLIENT_ORIGIN (confirmed, not wildcard)
- All backend tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/11-multi-provider-settings/11-02-SUMMARY.md`
</output>
