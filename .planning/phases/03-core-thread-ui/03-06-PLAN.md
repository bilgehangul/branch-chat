---
phase: 03-core-thread-ui
plan: 06
type: execute
wave: 4
depends_on:
  - 03-05
files_modified: []
autonomous: false
requirements:
  - CHAT-01
  - CHAT-02
  - CHAT-03
  - CHAT-04
  - CHAT-05
  - CHAT-06
  - NAV-01
  - NAV-02
  - NAV-03
  - NAV-04
  - NAV-05
  - NAV-06
  - NAV-07

must_haves:
  truths:
    - "User can send a message and watch Gemini respond token-by-token"
    - "AI response renders as formatted Markdown with syntax-highlighted code blocks"
    - "Streaming message appears at 80% opacity with no text selection possible"
    - "Stop button aborts the stream and input re-enables"
    - "Breadcrumb bar shows current thread path"
    - "All automated tests pass before human checkpoint"
---

<objective>
Human verification checkpoint: confirm the full Phase 3 feature set works correctly in the browser with a real Gemini SSE connection. Automated tests cannot verify streaming visual behavior, animation timing, or real AI responses.

Purpose: Gate on visual/functional correctness before declaring Phase 3 complete.
Output: Approved or list of issues to fix.
</objective>

<execution_context>
@C:/Users/bilge/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bilge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-core-thread-ui/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated suite before checkpoint</name>
  <files></files>
  <action>
Run the complete test suite and TypeScript check. Fix any failures before proceeding to the human checkpoint. Do NOT proceed to Task 2 if any automated check fails.

Commands:
1. `cd "C:/gmu/coding/GenAI Web interface/child_chats_v1/frontend" && npx vitest run --reporter=verbose`
2. `cd "C:/gmu/coding/GenAI Web interface/child_chats_v1/frontend" && npx tsc --noEmit`
3. `cd "C:/gmu/coding/GenAI Web interface/child_chats_v1/frontend" && npm run build`

All three must pass before the human checkpoint.
  </action>
  <verify>
    <automated>cd "C:/gmu/coding/GenAI Web interface/child_chats_v1/frontend" && npx vitest run --reporter=verbose 2>&1 | tail -10 && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>vitest exits 0, tsc exits 0, build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 3 chat interface:
- Streaming chat with Gemini (token-by-token SSE)
- GFM Markdown rendering with syntax-highlighted code blocks
- Animated typing dots + blinking cursor during streaming
- Stop button to abort stream mid-response
- Auto-scroll that follows new tokens but stops when scrolled up
- Breadcrumb bar showing thread path
- Left spine strip visible when in a child thread (Phase 4 will create child threads)
- 200ms slide-left transition when navigating to ancestors
  </what-built>
  <how-to-verify>
Start both servers:
- Backend: `cd "C:/gmu/coding/GenAI Web interface/child_chats_v1/backend" && npm run dev`
- Frontend: `cd "C:/gmu/coding/GenAI Web interface/child_chats_v1/frontend" && npm run dev`
- Open: http://localhost:5173

**Verification steps:**

1. **Sign in** — click sign in, authenticate. Verify you land on the chat interface (not a blank page or DemoChat). Breadcrumb should show "New Chat".

2. **Send a simple message** — type "Hello, who are you?" and click Send.
   - Expected: Typing dots appear immediately, then tokens stream in one-by-one
   - Expected: Message is at ~80% opacity while streaming
   - Expected: Blinking underscore `_` appears at end of streaming text
   - Expected: Full opacity returns when stream completes
   - Expected: Breadcrumb updates to show first words of your message

3. **Markdown rendering** — send: "Show me a Python hello world with an explanation in markdown"
   - Expected: Response renders with bold, code block (syntax highlighted), proper paragraph spacing
   - Expected: Code block has oneDark color scheme

4. **Multi-turn conversation** — send two more follow-up messages in the same thread
   - Expected: All messages appear in order; coherent conversation

5. **Stop button** — send a long-generation message ("Write a 1000 word essay on quantum computing") and immediately click Stop
   - Expected: Stream stops, input re-enables, Send button returns
   - Expected: Partial response is preserved in the message

6. **Auto-scroll** — send a message that generates a long response; during streaming, scroll up
   - Expected: Auto-scroll stops following new tokens when you scroll up
   - Expected: If you scroll back to bottom, auto-scroll resumes

7. **Breadcrumb bar** — verify "New Chat" → first message words transition. The current thread crumb should not be clickable.

8. **Spine strip** — at depth 0 (root thread), no spine strip should be visible. (Phase 4 will create child threads to test depth >= 1.)

9. **Sign out** — click UserButton → sign out. Verify DemoChat guest view returns (Zustand cleared).
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues found.</resume-signal>
</task>

</tasks>

<verification>
Human approval received or issues documented for fix.
</verification>

<success_criteria>
- All automated tests pass (vitest + tsc + build)
- Human confirms: streaming works token-by-token, Stop works, Markdown renders correctly, multi-turn conversation works, breadcrumb shows thread path
- No console errors during normal operation
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-thread-ui/03-06-SUMMARY.md`
</output>
