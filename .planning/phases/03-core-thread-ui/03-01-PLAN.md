---
phase: 03-core-thread-ui
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/tests/unit/useStreamingChat.test.ts
  - frontend/tests/unit/MarkdownRenderer.test.tsx
  - frontend/tests/unit/MessageBlock.test.tsx
  - frontend/tests/unit/ContextCard.test.tsx
  - frontend/tests/unit/BreadcrumbBar.test.tsx
  - frontend/tests/unit/SpineStrip.test.tsx
  - frontend/tests/unit/ThreadView.test.tsx
autonomous: true
requirements:
  - CHAT-01
  - CHAT-02
  - CHAT-03
  - CHAT-04
  - CHAT-05
  - CHAT-06
  - NAV-01
  - NAV-02
  - NAV-03
  - NAV-04
  - NAV-05
  - NAV-06
  - NAV-07

must_haves:
  truths:
    - "All 7 test stub files exist and pass (skipped tests count as passing)"
    - "Each test file imports from the correct future component path"
    - "No test file imports a module that does not yet exist (use vi.mock or conditional imports)"
  artifacts:
    - path: "frontend/tests/unit/useStreamingChat.test.ts"
      provides: "Test stubs for CHAT-01, CHAT-03, CHAT-04"
    - path: "frontend/tests/unit/MarkdownRenderer.test.tsx"
      provides: "Test stubs for CHAT-02"
    - path: "frontend/tests/unit/MessageBlock.test.tsx"
      provides: "Test stubs for CHAT-06"
    - path: "frontend/tests/unit/ContextCard.test.tsx"
      provides: "Test stubs for CHAT-05"
    - path: "frontend/tests/unit/BreadcrumbBar.test.tsx"
      provides: "Test stubs for NAV-01, NAV-02, NAV-03"
    - path: "frontend/tests/unit/SpineStrip.test.tsx"
      provides: "Test stubs for NAV-04, NAV-05"
    - path: "frontend/tests/unit/ThreadView.test.tsx"
      provides: "Test stubs for NAV-06, NAV-07"
  key_links:
    - from: "frontend/tests/unit/useStreamingChat.test.ts"
      to: "frontend/src/hooks/useStreamingChat.ts"
      via: "vi.mock + lazy import inside test body"
      pattern: "vi\\.mock.*useStreamingChat"
    - from: "frontend/tests/unit/BreadcrumbBar.test.tsx"
      to: "frontend/src/components/layout/BreadcrumbBar.tsx"
      via: "vi.mock or stub render"
      pattern: "BreadcrumbBar"
---

<objective>
Create all Wave 0 test stub files so every subsequent plan's verify command can run immediately without "cannot find module" failures. Stubs use `test.todo()` or `test.skip()` — they must pass (or skip) with zero failures before any implementation exists.

Purpose: Nyquist compliance — every CHAT and NAV requirement has an automated verify command wired before implementation begins.
Output: 7 test files with stub tests that collectively cover all 13 Phase 3 requirements.
</objective>

<execution_context>
@C:/Users/bilge/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bilge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-core-thread-ui/03-CONTEXT.md
@.planning/phases/03-core-thread-ui/03-VALIDATION.md

<interfaces>
<!-- Test infrastructure (from frontend/package.json) -->
<!-- Framework: Vitest 4.x + jsdom + @testing-library/react 16 -->
<!-- Quick run: cd frontend && npx vitest run tests/unit/ --reporter=verbose -->

<!-- All stub tests must use one of these patterns to avoid import errors: -->
<!--   1. test.todo('description') — no implementation needed, counts as skipped -->
<!--   2. vi.mock('../../src/hooks/useStreamingChat') to avoid importing non-existent module -->
<!--   3. Import from the correct FUTURE path even if the file does not exist yet, but use vi.mock() to intercept -->

<!-- Correct future component paths (create stubs targeting these): -->
<!--   frontend/src/hooks/useStreamingChat.ts -->
<!--   frontend/src/components/thread/MarkdownRenderer.tsx -->
<!--   frontend/src/components/thread/MessageBlock.tsx -->
<!--   frontend/src/components/thread/ContextCard.tsx -->
<!--   frontend/src/components/layout/BreadcrumbBar.tsx -->
<!--   frontend/src/components/layout/SpineStrip.tsx -->
<!--   frontend/src/components/thread/ThreadView.tsx -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create hook + message component test stubs (CHAT requirements)</name>
  <files>
    frontend/tests/unit/useStreamingChat.test.ts,
    frontend/tests/unit/MarkdownRenderer.test.tsx,
    frontend/tests/unit/MessageBlock.test.tsx,
    frontend/tests/unit/ContextCard.test.tsx
  </files>
  <action>
Create 4 test stub files. Each file must: (a) not fail due to missing module imports, (b) use test.todo() for all stubs so vitest counts them as pending (not failures).

**frontend/tests/unit/useStreamingChat.test.ts**
Import pattern: do NOT import the hook directly at the top level — the file doesn't exist yet. Instead write all tests as test.todo(). Cover:
- CHAT-01: `test.todo('sendMessage adds user message and empty AI placeholder to store')`
- CHAT-01: `test.todo('AI message content accumulates chunks from onChunk calls')`
- CHAT-03: `test.todo('sendMessage passes full thread message history to streamChat')`
- CHAT-04: `test.todo('hook works identically for child threads (multi-turn supported)')`
- Abort: `test.todo('abort() sets isStreaming=false on the AI message')`

**frontend/tests/unit/MarkdownRenderer.test.tsx**
Use test.todo() stubs. Cover CHAT-02:
- `test.todo('renders h1 heading from markdown')`
- `test.todo('renders bold and italic text from markdown')`
- `test.todo('renders fenced code block with syntax highlighting')`
- `test.todo('renders GFM table')`
- `test.todo('renders unordered list items')`

**frontend/tests/unit/MessageBlock.test.tsx**
Use test.todo() stubs. Cover CHAT-06:
- `test.todo('has user-select-none style when isStreaming is true')`
- `test.todo('has ~80% opacity when isStreaming is true')`
- `test.todo('restores full opacity when isStreaming is false')`
- `test.todo('renders role label "You" for user messages')`
- `test.todo('renders role label "Gemini" for assistant messages')`

**frontend/tests/unit/ContextCard.test.tsx**
Use test.todo() stubs. Cover CHAT-05:
- `test.todo('renders anchor text when thread.depth >= 1 and anchorText is set')`
- `test.todo('renders nothing when thread.depth === 0')`
- `test.todo('renders nothing when anchorText is null')`
  </action>
  <verify>
    <automated>cd "C:/gmu/coding/GenAI Web interface/child_chats_v1/frontend" && npx vitest run tests/unit/useStreamingChat.test.ts tests/unit/MarkdownRenderer.test.tsx tests/unit/MessageBlock.test.tsx tests/unit/ContextCard.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All 4 files run with 0 failures (todo tests show as pending, not failed). No "cannot find module" errors in output.</done>
</task>

<task type="auto">
  <name>Task 2: Create navigation component test stubs (NAV requirements)</name>
  <files>
    frontend/tests/unit/BreadcrumbBar.test.tsx,
    frontend/tests/unit/SpineStrip.test.tsx,
    frontend/tests/unit/ThreadView.test.tsx
  </files>
  <action>
Create 3 test stub files using test.todo() pattern.

**frontend/tests/unit/BreadcrumbBar.test.tsx**
Use test.todo() stubs. Cover NAV-01, NAV-02, NAV-03:
- `test.todo('renders a crumb for each thread in the ancestry chain')`
- `test.todo('renders "New Chat" label for root thread with no messages')`
- `test.todo('clicking an ancestor crumb calls setActiveThread with that thread id')`
- `test.todo('the current (last) crumb is not clickable')`
- `test.todo('collapses middle crumbs to ellipsis when ancestry.length > 3')`
- `test.todo('clicking ellipsis shows dropdown with all hidden crumbs')`

**frontend/tests/unit/SpineStrip.test.tsx**
Use test.todo() stubs. Cover NAV-04, NAV-05:
- `test.todo('renders when thread.depth >= 1')`
- `test.todo('does not render when thread.depth === 0')`
- `test.todo('displays parent thread title as vertical text')`
- `test.todo('applies parent thread accentColor as left border color')`
- `test.todo('clicking the strip calls setActiveThread with parentThreadId')`

**frontend/tests/unit/ThreadView.test.tsx**
Use test.todo() stubs. Cover NAV-06, NAV-07:
- `test.todo('applies slide-left CSS class when navigating to ancestor thread')`
- `test.todo('setScrollPosition is called with current scrollTop before navigating away')`
- `test.todo('scroll position is restored via requestAnimationFrame when activeThreadId changes')`
  </action>
  <verify>
    <automated>cd "C:/gmu/coding/GenAI Web interface/child_chats_v1/frontend" && npx vitest run tests/unit/BreadcrumbBar.test.tsx tests/unit/SpineStrip.test.tsx tests/unit/ThreadView.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All 3 files run with 0 failures. Full unit suite passes: `cd frontend && npx vitest run tests/unit/ --reporter=verbose` shows 0 failures (existing tests still green, new stubs pending).</done>
</task>

</tasks>

<verification>
`cd "C:/gmu/coding/GenAI Web interface/child_chats_v1/frontend" && npx vitest run tests/unit/ --reporter=verbose`

All 7 stub files present. Zero test failures. Existing tests (from Phase 2) remain green.
</verification>

<success_criteria>
- 7 test stub files exist at the correct paths
- `npx vitest run tests/unit/ --reporter=verbose` exits with code 0
- No "cannot find module" or parse errors
- All stubs use test.todo() — zero implementation written yet
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-thread-ui/03-01-SUMMARY.md`
</output>
