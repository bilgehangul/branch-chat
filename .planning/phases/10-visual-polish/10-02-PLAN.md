---
phase: 10-visual-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/history/SessionHistory.tsx
  - frontend/tests/unit/SessionHistory.test.tsx
autonomous: true
requirements: [SIDE-07, SIDE-08, SIDE-09, SIDE-10, SIDE-11, SIDE-12]
must_haves:
  truths:
    - "Thread tree uses SVG chevron icons with smooth CSS rotation animation"
    - "Each thread node shows its accent-color pip inline"
    - "Active thread row has left-border accent color and background tint"
    - "Thread tree has thin vertical and horizontal connecting lines (VS Code style)"
    - "3-dot menu appears on hover only with opacity transition"
    - "Delete confirmation uses centered modal dialog with backdrop"
  artifacts:
    - path: "frontend/src/components/history/SessionHistory.tsx"
      provides: "ThreadNode redesign with chevrons, pips, lines, modal delete"
    - path: "frontend/tests/unit/SessionHistory.test.tsx"
      provides: "Tests for accent pip, active thread, modal delete"
  key_links:
    - from: "SessionHistory.tsx ThreadNode"
      to: "theme.ts accentColor"
      via: "thread.accentColor for pip and active border"
      pattern: "accentColor"
---

<objective>
Redesign the thread tree in the sidebar with IDE-grade visual elements: chevron toggles with rotation animation, accent-color pips, VS Code-style connecting lines, hover-reveal 3-dot menus, and modal delete confirmation.

Purpose: Make the thread tree visually polished and interaction-rich, matching the VS Code file tree paradigm.
Output: Upgraded ThreadNode with all visual elements, DeleteModal component, tests.
</objective>

<execution_context>
@C:/Users/bilge/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bilge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-visual-polish/10-CONTEXT.md
@.planning/phases/10-visual-polish/10-RESEARCH.md

@frontend/src/components/history/SessionHistory.tsx
@frontend/src/theme.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread tree visual redesign (chevrons, pips, lines, hover menu)</name>
  <files>frontend/src/components/history/SessionHistory.tsx</files>
  <action>
    In ThreadNode component within SessionHistory.tsx:

    1. **Chevron icons (SIDE-07):** Replace Unicode triangle characters (▶/▼) with inline SVG chevron (path: "M6 4l4 4-4 4", w-3 h-3). Add `transition-transform duration-150` and `rotate-90` when expanded. Chevron visible only when thread has children.

    2. **Accent-color pip (SIDE-08):** Add a 6px circle span (w-1.5 h-1.5 rounded-full flex-shrink-0) inline after chevron, before thread title. Use `style={{ backgroundColor: thread.accentColor }}`.

    3. **Active thread highlight (SIDE-09):** Active thread row gets 2px left border using thread's own accentColor + background tint. Pattern: `border-l-2` with `style={{ borderLeftColor: thread.accentColor }}` + bg-zinc-800/50 dark / bg-stone-100 light.

    4. **Connecting lines (SIDE-10):** Thin solid lines (1px) showing hierarchy:
       - Vertical line: absolute-positioned border-l from parent to last child, using border-zinc-700 dark / border-stone-300 light
       - Horizontal stub: border-t from vertical line to node content
       - Position math: left = (depth - 1) * 16 + 7, consistent with existing 16px indent per level
       - Only render lines for depth > 0

    5. **3-dot hover reveal (SIDE-11):** Add `opacity-0 group-hover:opacity-100 transition-opacity` to ThreeDotButton. Wrap thread row in `group` class.

    Per user locked decisions: 16px indent per depth level, 1px solid zinc-700/stone-300 lines, 6-8px chevron with duration-150 rotation.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>Thread tree shows SVG chevrons with rotation, accent pips, connecting lines, active highlight with accent border, and hover-reveal 3-dot menus</done>
</task>

<task type="auto">
  <name>Task 2: Modal delete confirmation and tests</name>
  <files>frontend/src/components/history/SessionHistory.tsx, frontend/tests/unit/SessionHistory.test.tsx</files>
  <action>
    1. **DeleteModal component (SIDE-12):** Create a DeleteModal component (can be inside SessionHistory.tsx or extracted). Structure:
       - Fixed overlay: `fixed inset-0 z-50 flex items-center justify-center`
       - Backdrop: `absolute inset-0 bg-black/50` with onClick to cancel
       - Dialog: `relative bg-white dark:bg-zinc-800 rounded-lg shadow-xl p-6 max-w-sm mx-4`
       - Title: "Delete this thread?" (text-sm font-semibold)
       - Warning: "This will delete the thread and all child branches" (text-xs text-zinc-500)
       - Buttons: Cancel (neutral) + Delete (bg-red-500 text-white)
       - Props: open, onConfirm, onCancel, message
       - Use position:fixed to escape sidebar overflow context (per RESEARCH.md Pitfall 6)

    2. **Wire into existing delete flow:** Replace inline Yes/No confirmation with DeleteModal. Track `deleteTarget` state (thread ID or null). When 3-dot menu "Delete" clicked, set deleteTarget. Modal onConfirm calls existing delete handler then clears deleteTarget.

    3. **Tests (frontend/tests/unit/SessionHistory.test.tsx):**
       - Test: accent pip renders with correct backgroundColor style
       - Test: active thread has border-l-2 style
       - Test: DeleteModal renders when deleteTarget is set
       - Test: DeleteModal onConfirm calls delete handler
       - Test: DeleteModal onCancel clears dialog
  </action>
  <verify>
    <automated>cd frontend && npx vitest run tests/unit/SessionHistory.test.tsx -x</automated>
  </verify>
  <done>Delete uses centered modal with backdrop; tests verify accent pip, active highlight, and modal delete flow</done>
</task>

</tasks>

<verification>
- SessionHistory tests pass (new test file)
- Full test suite passes
- Visual: chevrons rotate smoothly, pips show thread colors, connecting lines align
- Visual: 3-dot menus fade in on hover, delete shows centered modal
</verification>

<success_criteria>
Thread tree has VS Code-style visual hierarchy with chevrons, pips, connecting lines, hover menus, and modal delete confirmation. All testable behaviors have passing unit tests.
</success_criteria>

<output>
After completion, create `.planning/phases/10-visual-polish/10-02-SUMMARY.md`
</output>
