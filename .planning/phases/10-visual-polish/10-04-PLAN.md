---
phase: 10-visual-polish
plan: 04
type: execute
wave: 2
depends_on: [10-03]
files_modified:
  - frontend/src/components/annotations/SimplificationBlock.tsx
  - frontend/src/components/annotations/CitationBlock.tsx
  - frontend/tests/unit/SimplificationBlock.test.tsx
  - frontend/tests/unit/CitationBlock.test.tsx
autonomous: true
requirements: [ANNO-06, ANNO-07, ANNO-08, ANNO-09, ANNO-10, ANNO-11, ANNO-12]
must_haves:
  truths:
    - "Annotation cards slide up and fade in on creation (200ms)"
    - "SimplificationBlock shows colored mode badge in header"
    - "Simplified text is rendered with MarkdownRenderer (not plain text)"
    - "Mode pills are always visible below content (no toggle needed)"
    - "CitationBlock defaults to expanded state"
    - "Citation sources show favicons, title links, snippet previews, and domain badges"
    - "Citation note styled as soft callout with icon"
  artifacts:
    - path: "frontend/src/components/annotations/SimplificationBlock.tsx"
      provides: "Mode badge, markdown rendering, always-visible pills, slide-up animation"
    - path: "frontend/src/components/annotations/CitationBlock.tsx"
      provides: "Default expanded, favicons, domain badges, callout note, slide-up animation"
    - path: "frontend/tests/unit/SimplificationBlock.test.tsx"
      provides: "Tests for mode badge, markdown rendering, visible pills"
    - path: "frontend/tests/unit/CitationBlock.test.tsx"
      provides: "Tests for default expanded, favicon, domain badge, callout"
  key_links:
    - from: "SimplificationBlock.tsx"
      to: "MarkdownRenderer"
      via: "import MarkdownRenderer with skipAnnotations=true"
      pattern: "skipAnnotations"
    - from: "CitationBlock.tsx"
      to: "Google S2 Favicons API"
      via: "img src with domain parameter"
      pattern: "google.com/s2/favicons"
---

<objective>
Enhance annotation cards: add enter animation (slide-up + fade), upgrade SimplificationBlock with mode badges, markdown rendering, and always-visible pills, and upgrade CitationBlock with default-expanded state, favicons, domain badges, and callout note styling.

Purpose: Make annotation cards feel polished and information-rich, with smooth entrance and better content display.
Output: Enhanced SimplificationBlock and CitationBlock with tests.
</objective>

<execution_context>
@C:/Users/bilge/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bilge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-visual-polish/10-CONTEXT.md
@.planning/phases/10-visual-polish/10-RESEARCH.md

@frontend/src/components/annotations/SimplificationBlock.tsx
@frontend/src/components/annotations/CitationBlock.tsx
@frontend/src/components/thread/MarkdownRenderer.tsx
@frontend/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: SimplificationBlock mode pills, markdown rendering, and badge</name>
  <files>frontend/src/components/annotations/SimplificationBlock.tsx, frontend/tests/unit/SimplificationBlock.test.tsx</files>
  <action>
    1. **Enter animation (ANNO-06):** Add `animate-slide-up-fade` class (from index.css, created in plan 03) to the SimplificationBlock wrapper div.

    2. **Mode badge (ANNO-07):** Add a small colored badge/tag next to "Simplified" label in the header. Badge text = active mode name (e.g., "ELI5", "Technical"). Badge styling: text-xs px-1.5 py-0.5 rounded-full, with mode-specific colors (indigo tint for active mode).

    3. **Markdown rendering (ANNO-08):** Replace plain text rendering of replacementText with MarkdownRenderer component. Pass `skipAnnotations={true}` prop to prevent infinite annotation nesting. Add the `skipAnnotations` prop to MarkdownRenderer if not already present (check MarkdownRenderer — when skipAnnotations is true, skip all annotationsAfter() calls and render plain markdown elements). Pass empty annotations array as an alternative approach.

    4. **Always-visible mode pills (ANNO-09):** Remove "Try another mode" toggle button. Always render the 4 mode pills in a horizontal row below the simplified content. Active mode pill: filled bg (bg-indigo-100 dark:bg-indigo-900/50 text-indigo-700 dark:text-indigo-300). Inactive pills: outlined (border border-zinc-300 dark:border-zinc-600, hover:bg-zinc-100 dark:hover:bg-zinc-800).

    5. **Tests (frontend/tests/unit/SimplificationBlock.test.tsx):**
       - Test: mode badge renders with active mode name
       - Test: MarkdownRenderer is used for replacement text (check for rendered markdown elements)
       - Test: all 4 mode pills are visible without a toggle
       - Test: active mode pill has filled styling
       - Test: wrapper has animate-slide-up-fade class
  </action>
  <verify>
    <automated>cd frontend && npx vitest run tests/unit/SimplificationBlock.test.tsx -x</automated>
  </verify>
  <done>SimplificationBlock has enter animation, mode badge in header, markdown-rendered content, and always-visible mode pills with active/inactive styling</done>
</task>

<task type="auto">
  <name>Task 2: CitationBlock default expanded, favicons, domain badges, and callout</name>
  <files>frontend/src/components/annotations/CitationBlock.tsx, frontend/tests/unit/CitationBlock.test.tsx</files>
  <action>
    1. **Enter animation (ANNO-06):** Add `animate-slide-up-fade` class to CitationBlock wrapper div.

    2. **Default expanded (ANNO-10):** Change the expanded state initialization from `useState(false)` to `useState(true)`. Keep the collapse/expand toggle (chevron flips) for user control.

    3. **Favicon per source (ANNO-11):** For each citation source, add:
       - 16px favicon: `<img src="https://www.google.com/s2/favicons?domain=${domain}&sz=16" alt="" width={16} height={16} className="flex-shrink-0" onError={(e) => { (e.target as HTMLImageElement).style.display = 'none' }} />`
       - Extract domain from source URL using `new URL(url).hostname`
       - Title as blue link: `<a href={url} target="_blank" rel="noopener noreferrer" className="text-blue-500 hover:underline text-sm">`
       - Snippet preview: truncate to ~120 chars, text-xs text-zinc-400
       - Domain badge: `<span className="text-xs px-2 py-0.5 rounded-full bg-stone-100 dark:bg-zinc-700 text-zinc-500 dark:text-zinc-400">{domain}</span>`

    4. **Citation note callout (ANNO-12):** Style the citation note as a soft callout box at the bottom:
       - Container: rounded-lg bg-zinc-100 dark:bg-zinc-800/50 p-3 mt-2
       - Speech bubble icon (inline SVG or Unicode) + text-sm text-zinc-500 italic
       - Renders the AI-generated citation note text

    5. **Tests (frontend/tests/unit/CitationBlock.test.tsx):**
       - Test: defaults to expanded state (content visible on mount)
       - Test: favicon img element has correct Google S2 URL format
       - Test: source title renders as link with target="_blank"
       - Test: domain badge renders with hostname text
       - Test: citation note renders in callout container
       - Test: wrapper has animate-slide-up-fade class
  </action>
  <verify>
    <automated>cd frontend && npx vitest run tests/unit/CitationBlock.test.tsx -x</automated>
  </verify>
  <done>CitationBlock defaults expanded, shows favicons + domain badges per source, has callout-styled citation note, and entrance animation</done>
</task>

</tasks>

<verification>
- SimplificationBlock tests pass
- CitationBlock tests pass
- Full test suite passes
- Visual: annotation cards slide up on creation
- Visual: SimplificationBlock shows badge, markdown content, visible pills
- Visual: CitationBlock is expanded with favicons, blue title links, domain badges, and callout note
</verification>

<success_criteria>
Annotation cards have smooth enter animations, SimplificationBlock shows rich mode controls with markdown content, and CitationBlock displays source information with favicons and domain badges in an expanded-by-default layout.
</success_criteria>

<output>
After completion, create `.planning/phases/10-visual-polish/10-04-SUMMARY.md`
</output>
