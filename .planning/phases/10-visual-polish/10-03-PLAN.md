---
phase: 10-visual-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/thread/MarkdownRenderer.tsx
  - frontend/src/components/thread/MessageBlock.tsx
  - frontend/src/components/thread/StreamingCursor.tsx
  - frontend/src/index.css
  - frontend/tests/unit/MarkdownRenderer.test.tsx
  - frontend/tests/unit/MessageBlock.test.tsx
autonomous: true
requirements: [MSGE-02, MSGE-03, MSGE-04, MSGE-05, MSGE-06, MSGE-07, MSGE-08, MSGE-09]
must_haves:
  truths:
    - "AI message headings have proper visual weight with font sizes, margins, and h2 has border-b"
    - "Code blocks have always-visible header bar with language label and copy button"
    - "Copy button shows Copied checkmark feedback for 2 seconds"
    - "Tables have subtle row striping in both dark and light modes"
    - "Blockquotes use dynamic thread accent color for left border"
    - "User messages preserve whitespace and show hover timestamps"
    - "Streaming cursor is a blinking vertical bar"
  artifacts:
    - path: "frontend/src/components/thread/MarkdownRenderer.tsx"
      provides: "Heading typography, CodeBlockWithCopy, table striping, blockquote accent, list spacing"
      exports: ["MarkdownRenderer"]
    - path: "frontend/src/components/thread/MessageBlock.tsx"
      provides: "User message whitespace-pre-wrap, hover timestamp"
    - path: "frontend/src/components/thread/StreamingCursor.tsx"
      provides: "Blinking pipe cursor"
    - path: "frontend/src/index.css"
      provides: "slideUpFade and blink keyframe animations"
  key_links:
    - from: "MarkdownRenderer.tsx blockquote"
      to: "accentColor prop"
      via: "inline style borderLeft with dynamic color"
      pattern: "accentColor"
    - from: "MessageBlock.tsx"
      to: "formatRelativeDate"
      via: "hover timestamp display"
      pattern: "formatRelativeDate"
---

<objective>
Polish message rendering: heading typography, code block copy-to-clipboard, table striping, accent-colored blockquotes, list spacing, user message timestamps, and streaming cursor animation.

Purpose: Make AI message content render with GitHub-quality typography and interactive code blocks, and improve user message display with timestamps.
Output: Enhanced MarkdownRenderer, MessageBlock with hover timestamps, StreamingCursor with pipe blink, CSS keyframes.
</objective>

<execution_context>
@C:/Users/bilge/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bilge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-visual-polish/10-CONTEXT.md
@.planning/phases/10-visual-polish/10-RESEARCH.md

@frontend/src/components/thread/MarkdownRenderer.tsx
@frontend/src/components/thread/MessageBlock.tsx
@frontend/src/components/thread/StreamingCursor.tsx
@frontend/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: MarkdownRenderer typography, code copy, table/list/blockquote enhancements</name>
  <files>frontend/src/components/thread/MarkdownRenderer.tsx, frontend/src/index.css, frontend/tests/unit/MarkdownRenderer.test.tsx</files>
  <action>
    **IMPORTANT:** Extract CodeBlockWithCopy as a separate named component OUTSIDE MarkdownRenderer to avoid re-render explosion (per RESEARCH.md Pitfall 1).

    In MarkdownRenderer.tsx component overrides:

    1. **Heading typography (MSGE-02):**
       - h1: text-xl font-bold mt-6 mb-2
       - h2: text-lg font-semibold mt-6 mb-2 + thin border-b (border-zinc-700 dark / border-stone-300 light)
       - h3: text-base font-semibold mt-4 mb-1

    2. **Code block copy (MSGE-03):** Create CodeBlockWithCopy component (defined OUTSIDE MarkdownRenderer):
       - Header bar: flex items-center justify-between, px-3 py-1.5, bg-zinc-800 dark / bg-zinc-700, text-xs text-zinc-400
       - Language label on left, copy button on right
       - Header always visible (not hover-only) per user locked decision
       - Click: navigator.clipboard.writeText, show checkmark + "Copied!" for 2 seconds via useState
       - Wrap in try/catch for clipboard API safety (per RESEARCH.md Pitfall 2)
       - Wire into MarkdownRenderer's `code` component override for block-level code (has className with language-)

    3. **List spacing (MSGE-04):** Add space-y-1.5 to ul and ol component overrides. Ensure nested lists have clear indentation.

    4. **Table striping (MSGE-05):** In tr component override, add `even:bg-stone-50 dark:even:bg-zinc-800/50`. Add min-w-full to table.

    5. **Blockquote accent (MSGE-06):** Add optional `accentColor` prop to MarkdownRenderer. Blockquote component uses inline style: `borderLeft: 3px solid ${accentColor || '#6B609A'}`. Add pl-4 italic text-zinc-400 dark:text-stone-500. Pass accentColor from MessageBlock (look up active thread's color from useSessionStore).

    6. **CSS keyframes in index.css:** Add slideUpFade animation (for annotation cards in plan 04) and blink animation for streaming cursor:
       ```
       @keyframes slideUpFade { from { opacity:0; transform:translateY(8px) } to { opacity:1; transform:translateY(0) } }
       .animate-slide-up-fade { animation: slideUpFade 200ms ease-out both; }
       @keyframes blink { 50% { opacity: 0 } }
       ```

    7. **Add tests** to existing MarkdownRenderer.test.tsx:
       - Test: h2 element has border-b class
       - Test: code block renders header with language label
       - Test: table tr has striping class
       - Test: blockquote has inline borderLeft style
  </action>
  <verify>
    <automated>cd frontend && npx vitest run tests/unit/MarkdownRenderer.test.tsx -x</automated>
  </verify>
  <done>Headings have proper typography, code blocks have copy header, tables have striping, blockquotes use accent color, lists have spacing</done>
</task>

<task type="auto">
  <name>Task 2: User message whitespace, hover timestamp, and streaming cursor</name>
  <files>frontend/src/components/thread/MessageBlock.tsx, frontend/src/components/thread/StreamingCursor.tsx, frontend/tests/unit/MessageBlock.test.tsx</files>
  <action>
    1. **User message whitespace (MSGE-07):** In MessageBlock.tsx, ensure user message content paragraph has `whitespace-pre-wrap break-words` classes. (Research notes whitespace-pre-wrap may already be partially present; add break-words.)

    2. **Hover timestamp (MSGE-08):** Add `group` class to user message bubble wrapper. Add a hidden timestamp element: `opacity-0 group-hover:opacity-100 transition-opacity text-xs text-zinc-500 mt-1`. Display formatRelativeDate(message.createdAt) — import from utils/formatRelativeDate.ts (created in plan 01). If formatRelativeDate not yet available (parallel wave), use a simple date format as fallback and note the import for wiring.

    3. **Streaming cursor (MSGE-09):** In StreamingCursor.tsx, change underscore character `_` to pipe `|`. Update animation to use `blink 1s step-end infinite` (CSS defined in index.css from Task 1). Keep existing bounce dots for empty state (no content yet) per user locked decision.

    4. **Pass accentColor to MarkdownRenderer:** In MessageBlock where MarkdownRenderer is called for assistant messages, pass the current thread's accentColor prop. Look up from useSessionStore: `threads[activeThreadId]?.accentColor`.

    5. **Add tests** to existing MessageBlock.test.tsx:
       - Test: user message has whitespace-pre-wrap and break-words classes
       - Test: user message shows timestamp element on hover (check element exists with relative date)
  </action>
  <verify>
    <automated>cd frontend && npx vitest run tests/unit/MessageBlock.test.tsx -x</automated>
  </verify>
  <done>User messages have whitespace-pre-wrap + break-words, hover shows relative timestamp, streaming cursor blinks as pipe character, accentColor flows to MarkdownRenderer</done>
</task>

</tasks>

<verification>
- MarkdownRenderer tests pass (heading, code copy, table, blockquote)
- MessageBlock tests pass (whitespace, timestamp)
- Full test suite passes
- Visual: headings have hierarchy, code blocks have copy headers, tables striped, blockquotes colored
- Visual: user messages show timestamps on hover, cursor blinks as |
</verification>

<success_criteria>
Message rendering matches GitHub-quality typography for AI messages and user messages show hover timestamps. Streaming cursor uses blinking pipe animation. CSS keyframes defined for phase-wide use.
</success_criteria>

<output>
After completion, create `.planning/phases/10-visual-polish/10-03-SUMMARY.md`
</output>
