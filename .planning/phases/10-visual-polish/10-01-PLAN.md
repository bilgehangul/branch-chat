---
phase: 10-visual-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/layout/AppShell.tsx
  - frontend/src/utils/formatRelativeDate.ts
  - frontend/src/components/history/SessionHistory.tsx
  - frontend/tests/unit/formatRelativeDate.test.ts
autonomous: true
requirements: [SIDE-01, SIDE-02, SIDE-03, SIDE-04, SIDE-05, SIDE-06]
must_haves:
  truths:
    - "Sidebar has subtle gradient background in both dark and light modes"
    - "Chats header is prominent with border separator"
    - "New Chat button is styled with rounded corners and hover elevation"
    - "Session entries have comfortable padding and accent-colored hover/active states"
    - "Session dates show smart relative format (Xm ago, Xh ago, Yesterday, etc.)"
  artifacts:
    - path: "frontend/src/utils/formatRelativeDate.ts"
      provides: "Shared relative date utility"
      exports: ["formatRelativeDate"]
    - path: "frontend/src/components/layout/AppShell.tsx"
      provides: "Sidebar gradient, header, New Chat styling"
    - path: "frontend/src/components/history/SessionHistory.tsx"
      provides: "Session entry hover/active states, relative dates"
  key_links:
    - from: "frontend/src/components/history/SessionHistory.tsx"
      to: "frontend/src/utils/formatRelativeDate.ts"
      via: "import formatRelativeDate"
      pattern: "formatRelativeDate"
    - from: "frontend/src/components/history/SessionHistory.tsx"
      to: "theme.ts accentColor"
      via: "root thread accentColor for active session border"
      pattern: "accentColor"
---

<objective>
Redesign sidebar visual foundation: gradient background, styled header and New Chat button, session entry hover/active states with accent-colored left bars, and smart relative date display.

Purpose: Transform the sidebar from plain to IDE-grade visual quality (VS Code feel) while adding useful relative date information.
Output: Styled sidebar with gradient, session entries with accent bars, formatRelativeDate utility with tests.
</objective>

<execution_context>
@C:/Users/bilge/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bilge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-visual-polish/10-CONTEXT.md
@.planning/phases/10-visual-polish/10-RESEARCH.md

@frontend/src/components/layout/AppShell.tsx
@frontend/src/components/history/SessionHistory.tsx
@frontend/src/theme.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create formatRelativeDate utility with tests</name>
  <files>frontend/src/utils/formatRelativeDate.ts, frontend/tests/unit/formatRelativeDate.test.ts</files>
  <behavior>
    - formatRelativeDate(date less than 1h ago) returns "Xm ago"
    - formatRelativeDate(date less than 24h ago) returns "Xh ago"
    - formatRelativeDate(date less than 48h ago) returns "Yesterday"
    - formatRelativeDate(date less than 7d ago) returns short day name ("Tue")
    - formatRelativeDate(date same year, older than 7d) returns "Mar 5"
    - formatRelativeDate(date different year) returns "Dec 15, 2025"
    - formatRelativeDate(0 minutes ago) returns "0m ago" or "just now"
  </behavior>
  <action>
    Write tests first in frontend/tests/unit/formatRelativeDate.test.ts covering the 6-tier format spec from CONTEXT.md. Then implement frontend/src/utils/formatRelativeDate.ts. The function accepts Date | string, returns string. No external date library per user locked decision.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run tests/unit/formatRelativeDate.test.ts -x</automated>
  </verify>
  <done>formatRelativeDate utility passes all 6+ test cases covering every tier of the relative date format</done>
</task>

<task type="auto">
  <name>Task 2: Sidebar gradient, header, button, and session entry styling</name>
  <files>frontend/src/components/layout/AppShell.tsx, frontend/src/components/history/SessionHistory.tsx</files>
  <action>
    In AppShell.tsx sidebar aside element:
    - Add gradient background via inline style: `background: linear-gradient(to bottom, ...)` with zinc-950 to zinc-900/80 dark, stone-50 to white light (use inline style to avoid Tailwind v4 gradient syntax uncertainty per RESEARCH.md Pitfall 7)
    - Style "Chats" header: text-lg font-semibold with border-b border-zinc-800 dark:border-stone-200
    - Style "+ New Chat" button: full-width, rounded-lg, bg-zinc-800 dark / stone-100 light, hover:bg-zinc-700 / hover:bg-stone-200, with Plus icon, transition-colors

    In SessionHistory.tsx session entries:
    - Add py-3 vertical padding to session buttons
    - Hover state: 2px left border with accent color + subtle bg tint (use group/group-hover pattern)
    - Active session: persistent 2px left bar using root thread's accentColor + stronger bg tint
    - Replace raw date with formatRelativeDate() import from utils
    - Use thread accentColor from the root thread for the session's accent bar

    Per user locked decisions: accent colors come from root thread's accentColor (theme.ts palette).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>Sidebar shows gradient bg, styled header and button; session entries have py-3 padding, accent-colored left bars on hover/active, relative dates</done>
</task>

</tasks>

<verification>
- formatRelativeDate unit tests pass
- Full test suite passes (no regressions)
- Visual: sidebar has gradient, header has border-b, button has rounded-lg styling
- Visual: session entries show relative dates and accent-colored hover/active bars
</verification>

<success_criteria>
Sidebar visuals match VS Code-inspired design with gradient, prominent controls, comfortable session entries with accent-colored indicators and smart relative dates.
</success_criteria>

<output>
After completion, create `.planning/phases/10-visual-polish/10-01-SUMMARY.md`
</output>
