---
phase: 04-branching
plan: "08"
type: execute
wave: 4
depends_on:
  - "04-06"
  - "04-07"
files_modified: []
autonomous: false
requirements:
  - BRANCH-01
  - BRANCH-02
  - BRANCH-03
  - BRANCH-04
  - BRANCH-05
  - BRANCH-06
  - BRANCH-07
  - BRANCH-08
  - BRANCH-09
  - BRANCH-10
  - BRANCH-11
  - BRANCH-12

must_haves:
  truths:
    - "User can select text in an AI response and see the action bubble appear"
    - "User can click Go Deeper and enter a child thread with a slide-right transition"
    - "Anchor paragraph shows a colored underline in the parent thread"
    - "Right gutter lead pill appears at the vertical position of the anchor paragraph"
    - "Lead pill shows thread title, message count, and accent pip"
    - "Clicking the lead pill navigates into the child thread"
    - "Breadcrumb bar and spine strip correctly reflect child thread depth"
    - "Go Deeper is disabled at depth 4 with tooltip"
  artifacts:
    - path: "frontend/src/components/branching/ActionBubble.tsx"
      provides: "verified functional in browser"
    - path: "frontend/src/components/branching/GutterColumn.tsx"
      provides: "verified pill alignment in browser"
  key_links: []
---

<objective>
Human verification of the complete Phase 4 branching feature. Verify all 12 BRANCH requirements are working end-to-end in the browser: text selection, action bubble, Go Deeper, child thread creation, gutter lead pills, navigation, and depth limit.

Purpose: Unit tests verify individual behaviors; this checkpoint verifies the integrated experience including animations, DOM positioning, and visual design.
Output: Phase 4 marked complete if approved; issues filed for gap closure if failures found.
</objective>

<execution_context>
@C:/Users/bilge/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bilge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-branching/04-CONTEXT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and start dev server</name>
  <files></files>
  <action>
Run the full test suite to confirm all automated checks pass:
```bash
cd "C:/gmu/coding/GenAI Web interface/child_chats_v1/frontend" && npx vitest run --reporter=verbose
```

If any tests fail, fix them before proceeding to the checkpoint.

Then start the development servers for manual verification:
```bash
# Terminal 1 (backend):
cd "C:/gmu/coding/GenAI Web interface/child_chats_v1/backend" && npm run dev

# Terminal 2 (frontend):
cd "C:/gmu/coding/GenAI Web interface/child_chats_v1/frontend" && npm run dev
```
  </action>
  <verify>
    <automated>cd "C:/gmu/coding/GenAI Web interface/child_chats_v1/frontend" && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>All automated tests pass; dev server running at http://localhost:5173</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 4 branching feature:
- Text selection → action bubble (BRANCH-01, BRANCH-02, BRANCH-03)
- Go Deeper → child thread creation with slide-right transition (BRANCH-04)
- Accent color cycling across child threads (BRANCH-05)
- Child thread title = first 6 words of selected text (BRANCH-06)
- Anchor paragraph colored underline in parent thread (BRANCH-07)
- Right gutter lead pill at anchor paragraph position (BRANCH-08, BRANCH-09)
- Lead pill hover preview card (BRANCH-10)
- Clicking lead pill navigates into child thread (BRANCH-11)
- Go Deeper disabled at depth 4 with "Maximum depth reached" tooltip (BRANCH-12)
  </what-built>
  <how-to-verify>
Open http://localhost:5173 and run through these scenarios in order:

**Scenario 1: Selection and bubble (BRANCH-01, BRANCH-02, BRANCH-03)**
1. Start a chat, wait for an AI response with multiple paragraphs
2. Click and drag to select text within ONE paragraph
3. EXPECTED: Action bubble appears within ~100ms above the selection's top-right corner
4. Verify bubble contains: "→ Go Deeper" (blue/primary), "🔍 Find Sources" (muted/disabled), "✦ Simplify" (muted/disabled)
5. Click outside the bubble — EXPECTED: bubble dismisses, selection cleared

**Scenario 2: Cross-block selection rejected (BRANCH-01)**
6. Try selecting text that spans TWO paragraphs (drag across paragraph boundary)
7. EXPECTED: No bubble appears; browser selection is cleared automatically

**Scenario 3: Go Deeper creates child thread (BRANCH-04, BRANCH-05, BRANCH-06)**
8. Select text in one paragraph, click "→ Go Deeper"
9. EXPECTED: Slide-right transition → child thread opens
10. EXPECTED: Breadcrumb bar shows "Root > [first 6 words of selected text]"
11. EXPECTED: Left spine strip appears showing parent thread title
12. EXPECTED: ContextCard at top of child thread shows anchor text with accent color left border
13. EXPECTED: First 6 words of selected text = thread title in breadcrumb

**Scenario 4: Anchor underline in parent (BRANCH-07)**
14. Navigate back to root thread (click breadcrumb or spine strip)
15. EXPECTED: The paragraph you selected shows a colored underline in the thread's accent color
16. Type a new AI message and wait for streaming — EXPECTED: underline persists after re-render

**Scenario 5: Gutter lead pills (BRANCH-08, BRANCH-09, BRANCH-10, BRANCH-11)**
17. In the root thread, look at the right side of the screen
18. EXPECTED: A 200px gutter appears with a lead pill positioned at the anchor paragraph's vertical location
19. EXPECTED: Pill shows "→" + truncated title (max 32 chars) + message count + colored pip
20. Hover the pill — EXPECTED: Preview card appears with anchor text and first exchange preview
21. Click the pill — EXPECTED: Slide-right into child thread

**Scenario 6: Depth limit (BRANCH-12)**
22. From a child thread (depth 1), Go Deeper into another paragraph → depth 2
23. Repeat until depth 4
24. At depth 4: select text, open bubble
25. EXPECTED: "→ Go Deeper" button is visually disabled (not clickable)
26. Hover/title on the disabled button — EXPECTED: tooltip "Maximum depth reached"

**Scenario 7: Multiple branches (BRANCH-05 cycling)**
27. Return to root thread, create a SECOND branch from a different paragraph
28. EXPECTED: Second child thread has a DIFFERENT accent color from the first
29. EXPECTED: Second lead pill appears in gutter at the second paragraph's position
  </how-to-verify>
  <resume-signal>Type "approved" if all scenarios pass, or describe which scenarios failed with details.</resume-signal>
</task>

</tasks>

<verification>
Manual verification as described in checkpoint. All 12 BRANCH requirements must be confirmed visually.
</verification>

<success_criteria>
All 7 scenarios pass in browser:
- Selection → bubble appears (BRANCH-01, BRANCH-02, BRANCH-03)
- Go Deeper → child thread with correct accent, title, transition (BRANCH-04, BRANCH-05, BRANCH-06)
- Anchor underline persists (BRANCH-07)
- Gutter pill at correct position with correct content (BRANCH-08, BRANCH-09)
- Hover preview (BRANCH-10)
- Pill click navigates (BRANCH-11)
- Depth 4 disabled with tooltip (BRANCH-12)
</success_criteria>

<output>
After human approval, create `.planning/phases/04-branching/04-08-SUMMARY.md`
</output>
