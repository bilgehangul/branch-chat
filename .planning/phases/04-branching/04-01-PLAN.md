---
phase: 04-branching
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/tests/unit/useTextSelection.test.ts
  - frontend/tests/unit/ActionBubble.test.tsx
  - frontend/tests/unit/GutterColumn.test.tsx
autonomous: true
requirements:
  - BRANCH-01
  - BRANCH-02
  - BRANCH-03
  - BRANCH-04
  - BRANCH-08
  - BRANCH-09
  - BRANCH-10
  - BRANCH-11
  - BRANCH-12

must_haves:
  truths:
    - "Three new test files exist and run without import errors"
    - "All tests are stubs (test.todo) — none are red/failing"
    - "unist-util-visit is importable from frontend/node_modules"
    - "@types/hast types are available for TypeScript"
  artifacts:
    - path: "frontend/tests/unit/useTextSelection.test.ts"
      provides: "Stub test file for BRANCH-01 and BRANCH-04"
    - path: "frontend/tests/unit/ActionBubble.test.tsx"
      provides: "Stub test file for BRANCH-02, BRANCH-03, BRANCH-12"
    - path: "frontend/tests/unit/GutterColumn.test.tsx"
      provides: "Stub test file for BRANCH-08, BRANCH-09, BRANCH-10, BRANCH-11"
  key_links:
    - from: "frontend/tests/unit/useTextSelection.test.ts"
      to: "frontend/src/hooks/useTextSelection.ts"
      via: "import (module must be created in wave 1)"
      pattern: "useTextSelection"
    - from: "frontend/tests/unit/GutterColumn.test.tsx"
      to: "frontend/src/components/branching/GutterColumn.tsx"
      via: "import (module must be created in wave 3)"
      pattern: "GutterColumn"
---

<objective>
Create Wave 0 test stubs for all new Phase 4 test files and verify that required npm dependencies (unist-util-visit, @types/hast) are available in the frontend. This plan unblocks all subsequent plans' automated verification commands.

Purpose: The Nyquist rule requires every verify command to have a test file. Three new test files cover requirements that no existing file addresses. Stubs use test.todo() so the suite stays green while modules don't exist yet.
Output: Three stub test files committed, deps verified or installed.
</objective>

<execution_context>
@C:/Users/bilge/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bilge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-branching/04-CONTEXT.md
@.planning/phases/04-branching/04-VALIDATION.md

<interfaces>
<!-- Established pattern from Phase 3: test.todo() used for stubs (not test.skip) — avoids import errors of non-existent modules -->
<!-- Established pattern from Phase 3: vi.mock hoisting requires test file separation per module -->

Existing test file pattern (from frontend/tests/unit/ContextCard.test.tsx):
```typescript
import { describe, it } from 'vitest';

describe('ComponentName', () => {
  it.todo('behavior description');
});
```

Phase 4 test → requirement mapping (from 04-VALIDATION.md):
- useTextSelection.test.ts → BRANCH-01 (selection capped to one block), BRANCH-04 (Go Deeper creates child thread)
- ActionBubble.test.tsx → BRANCH-02 (bubble appears in 100ms), BRANCH-03 (3 buttons), BRANCH-12 (disabled at depth 4)
- GutterColumn.test.tsx → BRANCH-08 (only when children > 0), BRANCH-09 (pill shows arrow/title/count/pip), BRANCH-10 (hover preview), BRANCH-11 (clicking navigates)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and install missing npm dependencies</name>
  <files>frontend/package.json</files>
  <action>
Check if unist-util-visit and @types/hast are available:

```bash
ls "C:/gmu/coding/GenAI Web interface/child_chats_v1/frontend/node_modules/unist-util-visit" 2>/dev/null && echo "available" || echo "missing"
ls "C:/gmu/coding/GenAI Web interface/child_chats_v1/frontend/node_modules/@types/hast" 2>/dev/null && echo "available" || echo "missing"
```

If unist-util-visit is missing, install as direct dependency:
```bash
cd "C:/gmu/coding/GenAI Web interface/child_chats_v1/frontend" && npm install unist-util-visit
```

If @types/hast is missing, install as dev dependency:
```bash
cd "C:/gmu/coding/GenAI Web interface/child_chats_v1/frontend" && npm install -D @types/hast
```

If both are already present (likely as transitives of remark-gfm), still add them as explicit direct/dev deps to prevent transitive brittleness. Update package.json accordingly.
  </action>
  <verify>
    <automated>cd "C:/gmu/coding/GenAI Web interface/child_chats_v1/frontend" && node -e "require.resolve('unist-util-visit')" && echo "ok"</automated>
  </verify>
  <done>unist-util-visit importable from frontend node_modules; @types/hast types available; both appear explicitly in package.json</done>
</task>

<task type="auto">
  <name>Task 2: Create Wave 0 test stub files</name>
  <files>
    frontend/tests/unit/useTextSelection.test.ts,
    frontend/tests/unit/ActionBubble.test.tsx,
    frontend/tests/unit/GutterColumn.test.tsx
  </files>
  <action>
Create three stub test files using test.todo() pattern. DO NOT import non-existent modules — stubs must be import-free so the suite stays green.

**frontend/tests/unit/useTextSelection.test.ts:**
```typescript
import { describe, it } from 'vitest';

describe('useTextSelection', () => {
  // BRANCH-01: selection boundary enforcement
  it.todo('returns null when selection is collapsed (click without drag)');
  it.todo('returns null when selection spans multiple [data-paragraph-id] blocks and clears the browser selection');
  it.todo('returns valid SelectionState with anchorText and paragraphId when selection is within one block');
  it.todo('returns null for whitespace-only selection');

  // BRANCH-04: Go Deeper wiring
  it.todo('clearBubble resets bubble state to null');
});
```

**frontend/tests/unit/ActionBubble.test.tsx:**
```typescript
import { describe, it } from 'vitest';

describe('ActionBubble', () => {
  // BRANCH-02: positioning and timing
  it.todo('renders at correct fixed position (top: bubble.top, left: bubble.left)');
  it.todo('applies transform translateY(calc(-100% - 8px)) for 8px above selection');

  // BRANCH-03: three action buttons
  it.todo('renders Go Deeper button with primary accent style');
  it.todo('renders Find Sources button with secondary/ghost style');
  it.todo('renders Simplify button with secondary/ghost style');
  it.todo('Find Sources button is disabled (Phase 5 not wired)');
  it.todo('Simplify button is disabled (Phase 5 not wired)');

  // BRANCH-12: depth limit
  it.todo('Go Deeper button is disabled when isAtMaxDepth is true');
  it.todo('Go Deeper button shows title tooltip "Maximum depth reached" when disabled');
  it.todo('Go Deeper button is enabled when isAtMaxDepth is false');

  // Bubble interaction
  it.todo('calls onGoDeeper with anchorText and paragraphId when Go Deeper is clicked');
  it.todo('calls onDismiss when clicking outside the bubble');
});
```

**frontend/tests/unit/GutterColumn.test.tsx:**
```typescript
import { describe, it } from 'vitest';

describe('GutterColumn', () => {
  // BRANCH-08: conditional rendering
  it.todo('renders nothing when thread has no childThreadIds');
  it.todo('renders the gutter column when thread has at least one childThreadId');

  // BRANCH-09: lead pill content
  it.todo('renders directional arrow (→) in each lead pill');
  it.todo('truncates thread title to 32 characters in lead pill');
  it.todo('displays live message count from thread.messageIds.length');
  it.todo('renders accent color pip using thread accentColor');

  // BRANCH-10: hover preview
  it.todo('shows preview card on pill hover with anchor text');
  it.todo('preview card shows first user message content');
  it.todo('preview card shows first line of first AI response');

  // BRANCH-11: navigation
  it.todo('clicking a lead pill calls setActiveThread with the correct threadId');
});
```

Each file uses only `{ describe, it }` from vitest — no other imports. This ensures zero import errors while modules don't exist yet.
  </action>
  <verify>
    <automated>cd "C:/gmu/coding/GenAI Web interface/child_chats_v1/frontend" && npx vitest run tests/unit/useTextSelection.test.ts tests/unit/ActionBubble.test.tsx tests/unit/GutterColumn.test.tsx --reporter=verbose 2>&1</automated>
  </verify>
  <done>All three stub files run without errors; all tests show as "todo" (not red/failed); full suite remains green</done>
</task>

</tasks>

<verification>
Run the full frontend test suite to confirm Wave 0 stubs do not break anything:
```bash
cd "C:/gmu/coding/GenAI Web interface/child_chats_v1/frontend" && npx vitest run --reporter=verbose
```
All existing tests pass. New stub files show todos only.
</verification>

<success_criteria>
- unist-util-visit and @types/hast listed as direct/dev dependencies in frontend/package.json
- Three stub test files exist with test.todo() stubs, no imports of non-existent modules
- Full vitest suite is green (no new failures)
- Subsequent plans' verify commands will find their target test files
</success_criteria>

<output>
After completion, create `.planning/phases/04-branching/04-01-SUMMARY.md`
</output>
