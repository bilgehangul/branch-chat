---
phase: 04-branching
plan: "07"
type: execute
wave: 3
depends_on:
  - "04-05"
files_modified:
  - frontend/src/components/thread/MessageList.tsx
  - frontend/src/components/thread/ContextCard.tsx
  - frontend/tests/unit/ContextCard.test.tsx
autonomous: true
requirements:
  - BRANCH-04
  - BRANCH-05

must_haves:
  truths:
    - "Child threads show a ContextCard at the top of the message list with the anchor text and parent message excerpt"
    - "ContextCard displays the thread accentColor as its left border or accent element"
    - "ContextCard is only shown when thread.depth >= 1 (not shown on root thread)"
  artifacts:
    - path: "frontend/src/components/thread/ContextCard.tsx"
      provides: "context banner at top of child threads showing anchor + parent context"
    - path: "frontend/src/components/thread/MessageList.tsx"
      provides: "renders ContextCard at top for child threads"
  key_links:
    - from: "frontend/src/components/thread/MessageList.tsx"
      to: "frontend/src/components/thread/ContextCard.tsx"
      via: "renders ContextCard when thread.depth >= 1"
      pattern: "depth.*ContextCard"
    - from: "frontend/src/components/thread/ContextCard.tsx"
      to: "frontend/src/types/index.ts"
      via: "receives thread.anchorText + thread.accentColor as props"
      pattern: "anchorText.*accentColor"
---

<objective>
Update ContextCard to display the thread's accentColor and be rendered from MessageList for all child threads (depth >= 1). This satisfies CHAT-05 (already in requirements) and ensures the branching visual identity (accent color) is present in child thread headers per BRANCH-05.

Purpose: ContextCard is the in-thread visual reminder of why this branch was created. Showing the accent color here completes the visual thread identity started in plan 05 (underline) and plan 06 (gutter pill pip).
Output: ContextCard updated with accentColor prop + MessageList renders it for child threads.
</objective>

<execution_context>
@C:/Users/bilge/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bilge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-branching/04-CONTEXT.md
@.planning/phases/04-branching/04-05-SUMMARY.md

<interfaces>
<!-- From frontend/src/types/index.ts -->
```typescript
export interface Thread {
  depth: 0 | 1 | 2 | 3 | 4;
  anchorText: string | null;
  parentMessageId: string | null;
  accentColor: string;
  title: string;
}
```

<!-- ContextCard already exists — check current implementation before modifying -->
<!-- Read frontend/src/components/thread/ContextCard.tsx before writing -->
<!-- Read frontend/src/components/thread/MessageList.tsx before writing -->

<!-- Design intent -->
// ContextCard should show:
// - Left border in accentColor (e.g., border-l-4 with inline style borderColor: accentColor)
// - "Context: [anchorText]" — what the user selected
// - Subtle header indicating this is a focused sub-conversation
// Style: bg-zinc-800/50 with accent left border; compact (not a big card)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update ContextCard with accentColor + render from MessageList</name>
  <files>
    frontend/src/components/thread/ContextCard.tsx,
    frontend/src/components/thread/MessageList.tsx,
    frontend/tests/unit/ContextCard.test.tsx
  </files>
  <behavior>
    - ContextCard accepts accentColor prop and applies it as the left border color
    - ContextCard shows the anchorText with a "focused sub-conversation" label
    - MessageList reads the active thread from the Zustand store
    - MessageList renders ContextCard at the top when thread.depth >= 1
    - ContextCard is NOT rendered for root threads (depth === 0)
  </behavior>
  <action>
First, READ the current ContextCard.tsx and MessageList.tsx files to understand their current shape before modifying.

Update `ContextCard.tsx` to accept and display accentColor:
- Add `accentColor: string` to props
- Apply as left border: `className="border-l-4 bg-zinc-800/50 rounded-r-lg px-4 py-3 mb-6"`, `style={{ borderColor: accentColor }}`
- Show anchor text with label: "Sub-conversation about:" as small muted label, anchorText in text-zinc-200
- Keep compact — max 2-3 lines of content

Update `MessageList.tsx` to render ContextCard when thread.depth >= 1:
- Import ContextCard
- Read `activeThread` from useSessionStore (or receive as prop if already threaded through — check current MessageList signature)
- If `thread.depth >= 1 && thread.anchorText`: render `<ContextCard anchorText={thread.anchorText} accentColor={thread.accentColor} />` before the message list

For ContextCard.test.tsx: update/extend tests to cover:
- Renders with accentColor as left border color
- Shows anchorText content
- Use @testing-library/react
  </action>
  <verify>
    <automated>cd "C:/gmu/coding/GenAI Web interface/child_chats_v1/frontend" && npx vitest run tests/unit/ContextCard.test.tsx --reporter=verbose 2>&1</automated>
  </verify>
  <done>ContextCard shows accentColor left border; MessageList renders ContextCard for depth >= 1 threads; ContextCard tests pass</done>
</task>

</tasks>

<verification>
```bash
cd "C:/gmu/coding/GenAI Web interface/child_chats_v1/frontend" && npx vitest run --reporter=verbose 2>&1
```
Full suite green.
</verification>

<success_criteria>
- ContextCard displays accentColor as left border using inline style (Tailwind cannot use runtime color values)
- MessageList renders ContextCard at top of message list for all child threads (depth >= 1)
- Root thread does not show ContextCard
- ContextCard tests updated and passing
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-branching/04-07-SUMMARY.md`
</output>
