---
phase: 04-branching
plan: "04"
type: execute
wave: 2
depends_on:
  - "04-02"
files_modified:
  - frontend/src/components/branching/ActionBubble.tsx
  - frontend/tests/unit/ActionBubble.test.tsx
autonomous: true
requirements:
  - BRANCH-02
  - BRANCH-03
  - BRANCH-12

must_haves:
  truths:
    - "ActionBubble renders at the correct fixed position (8px above top-right of selection)"
    - "ActionBubble contains Go Deeper (primary), Find Sources (secondary/disabled), Simplify (secondary/disabled)"
    - "Go Deeper is disabled with tooltip when isAtMaxDepth is true"
    - "Go Deeper fires onGoDeeper with anchorText and paragraphId when clicked"
    - "Bubble dismisses and selection clears when clicking outside"
  artifacts:
    - path: "frontend/src/components/branching/ActionBubble.tsx"
      provides: "floating selection action bubble component"
      exports: ["ActionBubble", "ActionBubbleProps"]
  key_links:
    - from: "frontend/src/components/branching/ActionBubble.tsx"
      to: "frontend/src/hooks/useTextSelection.ts"
      via: "receives SelectionState as props"
      pattern: "anchorText.*paragraphId.*top.*left"
    - from: "frontend/src/components/branching/ActionBubble.tsx"
      to: "frontend/src/store/selectors.ts"
      via: "isAtMaxDepth prop derived by parent"
      pattern: "isAtMaxDepth"
---

<objective>
Build the `ActionBubble` component — the floating action menu that appears after text selection. It displays three buttons (Go Deeper primary, Find Sources and Simplify secondary+disabled), positions itself 8px above the top-right of the selection using position:fixed, and disables Go Deeper at max depth.

Purpose: This is the visible UX surface for branching. The hook (plan 02) provides the state; this plan provides the component that consumes it.
Output: ActionBubble.tsx component + unit tests passing for BRANCH-02, BRANCH-03, BRANCH-12.
</objective>

<execution_context>
@C:/Users/bilge/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bilge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-branching/04-CONTEXT.md
@.planning/phases/04-branching/04-RESEARCH.md
@.planning/phases/04-branching/04-02-SUMMARY.md

<interfaces>
<!-- From frontend/src/hooks/useTextSelection.ts (created in plan 02) -->
```typescript
export interface SelectionState {
  anchorText: string;
  paragraphId: string;
  top: number;    // viewport Y from getBoundingClientRect().top
  left: number;   // viewport X from getBoundingClientRect().right
}
```

<!-- From frontend/src/store/selectors.ts -->
```typescript
export function isAtMaxDepth(thread: Thread): boolean {
  return thread.depth >= 4;
}
```

<!-- ActionBubble design decisions (locked in CONTEXT.md) -->
// Position: fixed, 8px above top-right of selection
// CSS: style={{ position: 'fixed', top: bubble.top, left: bubble.left, transform: 'translateY(calc(-100% - 8px))' }}
// Go Deeper: primary accent (blue highlight, visually dominant)
// Find Sources + Simplify: secondary ghost style, disabled in this phase
// Dismiss: on mousedown outside bubble (use useEffect with document mousedown listener)

<!-- Pitfall from 04-RESEARCH.md -->
// CRITICAL: onMouseDown on bubble buttons must call event.preventDefault() to prevent
// focus steal that collapses browser selection BEFORE click handler fires.
// ActionBubble reads anchorText/paragraphId from props (already captured in bubble state),
// NOT from window.getSelection() at click time.

<!-- Accent color for Go Deeper button -->
// Use Tailwind: bg-blue-600 hover:bg-blue-500 text-white (per CONTEXT.md: accent color treatment)
// Secondary buttons: bg-zinc-700 hover:bg-zinc-600 text-zinc-400 opacity-60 cursor-not-allowed
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build ActionBubble component</name>
  <files>
    frontend/src/components/branching/ActionBubble.tsx,
    frontend/tests/unit/ActionBubble.test.tsx
  </files>
  <behavior>
    - Renders at position: fixed with top=bubble.top, left=bubble.left, transform=translateY(calc(-100% - 8px))
    - "Go Deeper" button: bg-blue-600 style, calls onGoDeeper(anchorText, paragraphId) on click
    - "Find Sources" button: ghost/muted style, disabled attribute, cursor-not-allowed (Phase 5)
    - "Simplify" button: ghost/muted style, disabled attribute, cursor-not-allowed (Phase 5)
    - When isAtMaxDepth=true: Go Deeper button gets disabled attribute + title="Maximum depth reached"
    - When isAtMaxDepth=false: Go Deeper button is enabled, no title tooltip
    - All bubble buttons have onMouseDown={e => e.preventDefault()} to prevent focus steal
    - Clicking outside the bubble calls onDismiss
    - z-index: z-50 to appear above content
  </behavior>
  <action>
Create `frontend/src/components/branching/` directory and `ActionBubble.tsx`:

```typescript
export interface ActionBubbleProps {
  bubble: {
    anchorText: string;
    paragraphId: string;
    top: number;
    left: number;
  };
  isAtMaxDepth: boolean;
  onGoDeeper: (anchorText: string, paragraphId: string) => void;
  onDismiss: () => void;
}
```

Component implementation:
1. `useEffect` that attaches `mousedown` on document; if event.target is not inside the bubble container ref, calls `onDismiss()`. Clean up on unmount.
2. Outer div: `ref={bubbleRef}`, `className="fixed z-50 flex flex-col gap-1 bg-zinc-800 border border-zinc-700 rounded-lg shadow-xl p-2"`, `style={{ top: bubble.top, left: bubble.left, transform: 'translateY(calc(-100% - 8px))' }}`
3. Go Deeper button:
   - When enabled: `className="flex items-center gap-2 px-3 py-1.5 rounded-md bg-blue-600 hover:bg-blue-500 text-white text-sm font-medium transition-colors"`
   - When disabled (isAtMaxDepth): add `disabled opacity-50 cursor-not-allowed` classes, `title="Maximum depth reached"`
   - `onMouseDown={e => e.preventDefault()}` on ALL buttons
   - `onClick={() => onGoDeeper(bubble.anchorText, bubble.paragraphId)}` on Go Deeper
4. Find Sources button: `className="flex items-center gap-2 px-3 py-1.5 rounded-md bg-zinc-700 text-zinc-400 text-sm opacity-60 cursor-not-allowed"`, `disabled`
5. Simplify button: same as Find Sources

Each button shows: icon (use text emoji or simple unicode arrow — no icon library) + text label. Per CONTEXT.md: "Icon + text label for each button — clearest UX, no ambiguity."
- Go Deeper: "→ Go Deeper"
- Find Sources: "🔍 Find Sources"
- Simplify: "✦ Simplify"

For tests: replace test.todo stubs with real tests for:
- Renders at correct fixed position (check style attribute)
- Go Deeper button present with primary style class
- Find Sources and Simplify present with disabled attribute
- Go Deeper disabled + title when isAtMaxDepth=true
- Go Deeper enabled when isAtMaxDepth=false
- onGoDeeper called with correct args on click

Use @testing-library/react render with mock props.
  </action>
  <verify>
    <automated>cd "C:/gmu/coding/GenAI Web interface/child_chats_v1/frontend" && npx vitest run tests/unit/ActionBubble.test.tsx --reporter=verbose 2>&1</automated>
  </verify>
  <done>ActionBubble.tsx created; ActionBubble.test.tsx has passing tests for BRANCH-02, BRANCH-03, BRANCH-12; all stubs either promoted to real tests or remain as test.todo</done>
</task>

</tasks>

<verification>
```bash
cd "C:/gmu/coding/GenAI Web interface/child_chats_v1/frontend" && npx vitest run --reporter=verbose 2>&1
```
Full suite green. ActionBubble tests pass.
</verification>

<success_criteria>
- frontend/src/components/branching/ActionBubble.tsx created
- Positioned with position:fixed + transform:translateY(calc(-100% - 8px)) (8px above selection top-right)
- Three buttons: Go Deeper (primary blue), Find Sources (disabled ghost), Simplify (disabled ghost)
- Go Deeper disabled with tooltip at max depth (BRANCH-12)
- onMouseDown preventDefault on all buttons (pitfall 1 prevention)
- Dismiss on outside click via document mousedown listener
- Unit tests cover all three requirement behaviors
</success_criteria>

<output>
After completion, create `.planning/phases/04-branching/04-04-SUMMARY.md`
</output>
