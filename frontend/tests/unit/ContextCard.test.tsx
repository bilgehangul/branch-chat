/**
 * Test stubs for ContextCard component
 * Requirements: CHAT-05
 *
 * All tests use test.todo() — no implementation exists yet.
 * This file must pass (with pending stubs) before the component is implemented.
 */

import { describe } from 'vitest';

describe('ContextCard', () => {
  // CHAT-05: Anchor text display
  test.todo('renders anchor text when thread.depth >= 1 and anchorText is set');
  test.todo('renders nothing when thread.depth === 0');
  test.todo('renders nothing when anchorText is null');
});
